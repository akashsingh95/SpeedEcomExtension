// amazon_background.js - generated by build.js from Amazon. Do not edit.
// Body below is the original file, verbatim.
(function () {
'use strict';
const chrome = globalThis.__seShim("amz", ["syncProgress","syncLog"]);

'use strict';


const TAG = '[amazon-sync:bg]';

const MAX_ATTEMPTS = 3;              // 1 initial try + 2 retries
const LOG_MAX_ENTRIES = 200;
const TICK_ALARM_NAME = 'amazon-order-sync-tick';
const PAYMENTS_TICK_ALARM_NAME = 'amazon-payments-sync-tick';
const GST_TICK_ALARM_NAME = 'amazon-gst-sync-tick';
const ADS_TICK_ALARM_NAME = 'amazon-ads-sync-tick';
const BULK_TICK_ALARM_NAME = 'amazon-bulk-sync-tick';
const TICK_INTERVAL_MINUTES = 0.5;   // Chrome's own alarm floor - matches the old 30s poll interval

// ── Auth: SpeedEcom account login ───────────────────────────────────────────
// Same flow and same backend as the Meesho/Myntra extensions: GET public-key
// (PEM) -> RSA-OAEP/SHA-1 encrypt the password -> POST login (email +
// encrypted password) -> GET profile with the bearer token. Token is cached
// in chrome.storage.local indefinitely - nothing here clears it automatically.
const AUTH_ORIGIN = 'https://speedecomsolution.in';

// Production's CORS check rejects requests whose Origin isn't its own web
// app (the extension's real origin, chrome-extension://<id>, isn't on that
// allow-list). Fetch can't set Origin/Referer directly - forbidden headers -
// so this rewrites them at the network layer for AUTH_ORIGIN requests only,
// making the request look like it came from the site's own login page.
const AUTH_HEADER_RULE_ID = 101;
chrome.declarativeNetRequest.updateSessionRules({
  removeRuleIds: [AUTH_HEADER_RULE_ID],
  addRules: [
    {
      id: AUTH_HEADER_RULE_ID,
      priority: 1,
      action: {
        type: 'modifyHeaders',
        requestHeaders: [
          { header: 'Origin', operation: 'set', value: AUTH_ORIGIN },
          { header: 'Referer', operation: 'set', value: `${AUTH_ORIGIN}/client/login` },
        ],
      },
      condition: {
        urlFilter: `||${AUTH_ORIGIN.replace(/^https?:\/\//, '')}/api/auth/*`,
        resourceTypes: ['xmlhttprequest'],
      },
    },
  ],
}).catch((e) => console.warn(TAG, 'declarativeNetRequest rule failed:', e?.message));

function pemToArrayBuffer(pem) {
  const b64 = String(pem)
    .replace(/-----BEGIN PUBLIC KEY-----/, '')
    .replace(/-----END PUBLIC KEY-----/, '')
    .replace(/[\r\n\s]+/g, '');
  const binary = atob(b64);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
  return bytes.buffer;
}

function arrayBufferToBase64(buf) {
  const bytes = new Uint8Array(buf);
  let binary = '';
  for (let i = 0; i < bytes.length; i++) binary += String.fromCharCode(bytes[i]);
  return btoa(binary);
}

async function encryptPassword(password, pem) {
  const keyData = pemToArrayBuffer(pem);
  const key = await crypto.subtle.importKey('spki', keyData, { name: 'RSA-OAEP', hash: 'SHA-1' }, false, ['encrypt']);
  const cipher = await crypto.subtle.encrypt({ name: 'RSA-OAEP' }, key, new TextEncoder().encode(password));
  return arrayBufferToBase64(cipher);
}

// Defensive extraction - the exact response shape isn't documented, so
// search for a JWT-looking string rather than hard-coding one field name.
function extractToken(body) {
  const isJwt = (v) => typeof v === 'string' && /^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/.test(v);
  const direct = body?.token || body?.accessToken || body?.access_token || body?.jwt ||
    body?.data?.token || body?.data?.accessToken || body?.result?.token;
  if (isJwt(direct)) return direct;
  const stack = [body];
  while (stack.length) {
    const cur = stack.shift();
    if (!cur || typeof cur !== 'object') continue;
    for (const v of Object.values(cur)) {
      if (isJwt(v)) return v;
      if (v && typeof v === 'object') stack.push(v);
    }
  }
  return null;
}

function extractUser(body) {
  const u = body?.data?.user || body?.user || body?.data || body;
  if (!u || typeof u !== 'object') return {};
  const name = u.name || u.fullName || u.full_name || u.business_name || u.businessName || u.company || u.displayName || u.username || null;
  const email = u.email || u.emailAddress || u.email_address || null;
  // The logged-in user's own `name` is just their personal login account name
  // (e.g. "demo1") - the seller/company name registered with SpeedEcom lives
  // on the populated tenant record instead (`GET /api/auth/profile`'s
  // `tenantId.name`, per server/controllers/authController.js's getProfile).
  const tenant = u.tenantId?.name || u.tenant?.name || null;
  return { name, email, tenant };
}

async function authLogin(email, password) {
  const pkRes = await fetch(`${AUTH_ORIGIN}/api/auth/public-key`, { headers: { accept: 'application/json' } });
  const pkBody = await pkRes.json().catch(() => ({}));
  if (!pkRes.ok || !pkBody?.publicKey) throw new Error(pkBody?.message || 'Could not reach login server.');

  const encryptedPassword = await encryptPassword(password, pkBody.publicKey);

  const loginRes = await fetch(`${AUTH_ORIGIN}/api/auth/login`, {
    method: 'POST',
    headers: { 'content-type': 'application/json', accept: 'application/json, text/plain, */*' },
    body: JSON.stringify({ email, password: encryptedPassword }),
  });
  const loginBody = await loginRes.json().catch(() => ({}));
  if (!loginRes.ok) throw new Error(loginBody?.message || 'Invalid email or password.');

  const token = extractToken(loginBody);
  if (!token) throw new Error('Login succeeded but no session token was returned.');

  const user = await authFetchProfile(token);
  await chrome.storage.local.set({ auth: { token, user, loggedInAt: Date.now() } });
  return user;
}

async function authFetchProfile(token) {
  const res = await fetch(`${AUTH_ORIGIN}/api/auth/profile`, {
    headers: { accept: 'application/json, text/plain, */*', authorization: `Bearer ${token}` },
  });
  const body = await res.json().catch(() => ({}));
  if (!res.ok) throw new Error(body?.message || 'Could not load profile.');
  return extractUser(body);
}

// Toolbar-icon badge - lets a running/finished sync be noticed without
// having the popup open at all. Badge text is stateful (unlike a popup),
// so it deliberately stays as-is (✓ or !) after a sync finishes until the
// popup is actually opened and acknowledges it (see the CLEAR_BADGE message,
// sent right after initMainApp() clears a finished syncProgress) - clearing
// it the instant the sync finishes would defeat the point of a passive
// "something happened" signal.
async function updateBadge(progress) {
  try {
    if (!progress || (!progress.active && !progress.finished)) {
      await chrome.action.setBadgeText({ text: '' });
      return;
    }
    if (progress.finished) {
      if (progress.error) {
        await chrome.action.setBadgeText({ text: '!' });
        await chrome.action.setBadgeBackgroundColor({ color: '#dc2626' });
      } else if (progress.cancelled) {
        await chrome.action.setBadgeText({ text: '' });
      } else {
        await chrome.action.setBadgeText({ text: '✓' });
        await chrome.action.setBadgeBackgroundColor({ color: '#16a34a' });
      }
      return;
    }
    // Active: show a done/total count once downloading is actually under
    // way (meaningful progress), otherwise a plain dot for the
    // scheduling/waiting-for-Amazon phases where there's nothing to count yet.
    const text = progress.total > 0 ? `${progress.done}/${progress.total}` : '•';
    await chrome.action.setBadgeText({ text: String(text).slice(0, 4) });
    await chrome.action.setBadgeBackgroundColor({ color: '#FF9900' });
  } catch (_) {}
}

// Powers the popup's small "Last synced: ..." summary line - a single most-
// recent-run record (not history), overwritten by whichever completion
// point calls this next, regardless of which tab/engine it came from. Only
// called for runs that actually produced at least one file - a total
// failure isn't something worth remembering as "the last sync".
async function recordLastSync(label, state) {
  if (!state || state.cancelled || !(state.done > 0)) return;
  try {
    await chrome.storage.local.set({
      lastSync: { label, done: state.done, total: state.total, failed: state.failed || 0, at: Date.now() },
    });
  } catch (_) {}
}

// Native OS notification the moment a sync actually finishes - each tick
// engine only ever sets finished:true once per run (the job is cleared
// immediately after, so no later tick can re-fire it), which is what makes
// hooking this straight into setProgress safe without needing to track
// "was this already notified" state separately. Most valuable for long Bulk
// runs where the popup isn't sitting open the whole time, but fires either
// way - a closed popup is exactly when this is the only signal available.
async function notifySyncResult(progress) {
  if (!progress?.finished || progress.cancelled) return;
  try {
    const id = `amazon-sync-${Date.now()}`;
    if (progress.error) {
      await chrome.notifications.create(id, {
        type: 'basic',
        iconUrl: 'icons/icon128.png',
        title: 'Amazon sync failed',
        message: progress.error,
        priority: 1,
      });
    } else {
      const doneText = progress.total > 0 ? `${progress.done} file${progress.done === 1 ? '' : 's'} downloaded` : 'Download complete';
      const failText = progress.failed > 0 ? ` - ${progress.failed} failed` : '';
      await chrome.notifications.create(id, {
        type: 'basic',
        iconUrl: 'icons/icon128.png',
        title: 'Amazon sync complete',
        message: `${doneText}${failText}`,
        priority: 0,
      });
    }
  } catch (_) {}
}

async function setProgress(progress) {
  try { await chrome.storage.local.set({ syncProgress: { ...progress, at: Date.now() } }); } catch (_) {}
  await updateBadge(progress);
  await notifySyncResult(progress);
}

function newState(overrides) {
  return { active: true, total: 0, done: 0, failed: 0, failures: [], current: null, finished: false, error: null, paused: false, cancelled: false, ...overrides };
}

const sleep = (ms) => new Promise((r) => setTimeout(r, ms));

async function appendLog(entry) {
  const line = { ...entry, at: Date.now() };
  console.log(TAG, line.status, line.filename || '', line.detail || '');
  try {
    const { syncLog } = await chrome.storage.local.get('syncLog');
    const list = Array.isArray(syncLog) ? syncLog : [];
    list.push(line);
    while (list.length > LOG_MAX_ENTRIES) list.shift();
    await chrome.storage.local.set({ syncLog: list });
  } catch (_) {}
}

function base64ToArrayBuffer(base64) {
  const binary = atob(base64);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
  return bytes.buffer;
}

// ── Resumable job state ─────────────────────────────────────────────────────
// Everything needed to continue the sync lives in chrome.storage.local, not
// in a JS variable - a plain in-memory flag (the previous design) is wiped
// out the instant the service worker is reclaimed, which MV3 does after
// ~30s of no active work. An `await` sitting on a report-generation wait of
// up to 45 minutes looks exactly like "no active work" to Chrome, even
// though chrome.alarms is being used specifically to survive that - the
// alarm firing later does wake the worker back up, but only by re-running
// background.js from scratch, which has nothing left to resume the original
// long-lived async chain. That's what silently orphaned the sync and made
// Pause/Cancel stop doing anything once the worker had been reclaimed once -
// closing/reopening the popup didn't cause it, it just made it visible.
//
// The fix: persist job state here, and drive it forward with a single
// fixed-name recurring alarm whose listener is registered unconditionally
// at the top of this file - so it's always re-armed the instant the worker
// restarts for any reason, and Pause/Cancel are just storage writes that
// the next tick (which can run within moments, not up to 30 minutes later)
// will actually see.
async function getOrderJob() {
  const { orderJob } = await chrome.storage.local.get('orderJob');
  return orderJob || null;
}
async function setOrderJob(job) {
  await chrome.storage.local.set({ orderJob: job });
}
async function clearOrderJob() {
  await chrome.storage.local.remove('orderJob');
  await chrome.alarms.clear(TICK_ALARM_NAME);
}

async function getPaymentsJob() {
  const { paymentsJob } = await chrome.storage.local.get('paymentsJob');
  return paymentsJob || null;
}
async function setPaymentsJob(job) {
  await chrome.storage.local.set({ paymentsJob: job });
}
async function clearPaymentsJob() {
  await chrome.storage.local.remove('paymentsJob');
  await chrome.alarms.clear(PAYMENTS_TICK_ALARM_NAME);
}

async function getGstJob() {
  const { gstJob } = await chrome.storage.local.get('gstJob');
  return gstJob || null;
}
async function setGstJob(job) {
  await chrome.storage.local.set({ gstJob: job });
}
async function clearGstJob() {
  await chrome.storage.local.remove('gstJob');
  await chrome.alarms.clear(GST_TICK_ALARM_NAME);
}

async function getAdsJob() {
  const { adsJob } = await chrome.storage.local.get('adsJob');
  return adsJob || null;
}
async function setAdsJob(job) {
  await chrome.storage.local.set({ adsJob: job });
}
async function clearAdsJob() {
  await chrome.storage.local.remove('adsJob');
  await chrome.alarms.clear(ADS_TICK_ALARM_NAME);
}

async function getBulkJob() {
  const { bulkJob } = await chrome.storage.local.get('bulkJob');
  return bulkJob || null;
}
async function setBulkJob(job) {
  await chrome.storage.local.set({ bulkJob: job });
}
async function clearBulkJob() {
  await chrome.storage.local.remove('bulkJob');
  await chrome.alarms.clear(BULK_TICK_ALARM_NAME);
}

// Only one sync (any single category, or one Bulk run) is ever meant to run
// at a time - the popup disables all Sync buttons together while any one is
// active. This resolves to whichever job actually exists, so
// pause/cancel/download-queue logic doesn't need to know or care which
// category (or bulk run) is currently active. Standard Returns/FBA Returns
// no longer have their own separate job store - the individual Returns tab
// now runs through the Bulk engine (getBulkJob) scoped to just those
// categories, since that engine already generalizes to any subset and
// already carries the navigation-orchestration those two need.
async function getActiveJob() {
  return (await getOrderJob()) || (await getPaymentsJob()) || (await getGstJob()) || (await getAdsJob()) || (await getBulkJob());
}

// Every file here comes back through content.js's relay (base64-encoded) -
// unlike Myntra, there's no separate blob host or redirect to worry about,
// since documentMetadata (Orders), the Returns download links, Payments'
// download-report, and GST's ondemand-download are all same-origin with the
// content script that fetches them. task.kind picks which relay message
// shape to send - Orders and Payments both track a reportId/referenceId
// they resolve themselves, Returns and GST already have everything needed
// (a full URL, or documentId+dateRangeCovered+reportType) handed to them by
// the parsed list/poll response.
// The GST ondemand-download endpoint's response is itself a ZIP archive
// (starts with the PK signature) containing one real CSV inside - confirmed
// live: a file saved straight through with a plain .csv extension opened as
// raw zip bytes/garbage in Sheets. Unzipped here so the outer ZIP this
// extension builds ends up with one flat, actually-openable CSV rather than
// a zip nested inside a zip.
async function extractGstCsvFromZip(buf) {
  const inner = await JSZip.loadAsync(buf);
  const entries = Object.values(inner.files).filter((f) => !f.dir);
  if (!entries.length) throw new Error('The GST report ZIP was empty.');
  return entries[0].async('arraybuffer');
}

async function fetchFileOnce(task, tabId) {
  let msg;
  if (task.kind === 'returns') msg = { type: 'FETCH_RETURNS_FILE', url: task.url };
  else if (task.kind === 'payments') msg = { type: 'FETCH_PAYMENTS_FILE', reportId: task.reportId };
  else if (task.kind === 'gst') msg = { type: 'FETCH_GST_FILE', documentId: task.documentId, dateRangeCovered: task.dateRangeCovered, reportType: task.reportType };
  else if (task.kind === 'fbaReturns') msg = { type: 'FETCH_FBA_RETURNS_FILE', referenceId: task.referenceId };
  else msg = { type: 'FETCH_ORDER_FILE', referenceId: task.referenceId };
  const resp = await sendToTabWithRecovery(tabId, msg);
  if (!resp?.ok) throw new Error(resp?.error || 'Could not fetch the report file.');
  if (resp.status >= 400) throw new Error(`HTTP ${resp.status}`);
  if (!resp.base64) throw new Error('Empty file response.');
  const buf = base64ToArrayBuffer(resp.base64);
  if (task.kind === 'gst') return extractGstCsvFromZip(buf);
  return buf;
}

// Retries a single task up to MAX_ATTEMPTS on failure (network error, bad
// status) - never throws, always resolves with a result so the queue can
// continue to the next task regardless.
async function fetchFileWithRetry(task, tabId) {
  let lastErr;
  for (let attempt = 1; attempt <= MAX_ATTEMPTS; attempt++) {
    try {
      const buf = await fetchFileOnce(task, tabId);
      await appendLog({ status: 'downloaded', filename: task.filename });
      return { ok: true, buf };
    } catch (e) {
      lastErr = e;
      const isFinal = attempt >= MAX_ATTEMPTS;
      await appendLog({ status: isFinal ? 'failed' : 'retrying', filename: task.filename, detail: e.message, attempt });
    }
  }
  return { ok: false, error: lastErr?.message || 'Unknown error' };
}

function uniqueZipName(used, name) {
  let n = (name && String(name).trim()) || 'file';
  if (!used.has(n)) { used.add(n); return n; }
  const dot = n.lastIndexOf('.');
  const base = dot > 0 ? n.slice(0, dot) : n;
  const ext = dot > 0 ? n.slice(dot) : '';
  let i = 2;
  while (used.has(`${base} (${i})${ext}`)) i++;
  const out = `${base} (${i})${ext}`;
  used.add(out);
  return out;
}

// Sequential by design - fetches one file at a time, packs everything into
// one JSZip, and triggers exactly one chrome.downloads.download() call for
// the whole batch. Today Orders only ever produces a single file per sync,
// but this stays generic/task-based so Payments/Ads/Returns can plug into
// the exact same engine once those are built.
//
// Pause/Cancel here re-read the active job from storage on every iteration
// rather than trusting an in-memory flag, for the same reason described
// above - each individual file fetch is fast enough that this phase is far
// less likely to actually hit the 30s idle-suspend window than the
// multi-minute report-generation wait, but a long Pause held here for
// several minutes could in theory still hit it. That's an accepted, narrow
// gap for now (Orders only ever has one file), not something silently
// ignored - worth revisiting if Payments/Ads/Returns ever produce large
// multi-file batches where a long pause mid-download becomes realistic.
async function processDownloadQueue(tasks, zipBaseName, tabId) {
  const state = newState({ total: tasks.length });
  await setProgress(state);

  const zip = new JSZip();
  const used = new Set();

  for (const task of tasks) {
    let job = await getActiveJob();
    while (job?.paused && !job?.cancelled) {
      state.paused = true;
      state.current = 'Paused';
      await setProgress(state);
      await sleep(300);
      job = await getActiveJob();
    }
    state.paused = false;

    if (job?.cancelled) { state.cancelled = true; break; }

    state.current = task.filename;
    await setProgress(state);

    const result = await fetchFileWithRetry(task, tabId);
    if (result.ok) {
      const zipPath = task.zipFolder ? `${task.zipFolder}/${task.filename}` : task.filename;
      zip.file(uniqueZipName(used, zipPath), result.buf);
      state.done++;
    } else {
      state.failed++;
      // task.cat (only set by bulkSyncTick/gstSyncTick, which know their own
      // category/type keys) lets a "Retry failed only" button rebuild a
      // scoped retry request straight from this failure list - undefined
      // and harmless for every other single-purpose sync engine.
      state.failures.push({ filename: task.filename, reason: result.error, cat: task.cat });
    }
    await setProgress(state);
  }

  if (state.done > 0 && !state.cancelled) {
    state.current = 'Generating ZIP...';
    await setProgress(state);

    // URL.createObjectURL is not available in this MV3 service worker
    // context, so JSZip generates a base64 string directly instead of a
    // Blob, and that becomes a data: URL - chrome.downloads.download()
    // accepts data: URLs natively.
    const zipBase64 = await zip.generateAsync({ type: 'base64', compression: 'DEFLATE', compressionOptions: { level: 6 } });
    const dataUrl = `data:application/zip;base64,${zipBase64}`;
    await new Promise((resolve) => {
      chrome.downloads.download({ url: dataUrl, filename: `${zipBaseName}.zip`, saveAs: false }, (id) => {
        if (chrome.runtime.lastError) console.warn(TAG, 'zip download:', chrome.runtime.lastError.message);
        resolve(id);
      });
    });
  }

  if (state.cancelled) {
    const remaining = tasks.length - state.done - state.failed;
    state.current = `Cancelled - stopped after ${state.done} file(s) fetched, ${remaining} not attempted. No ZIP was created.`;
  } else {
    state.current = null;
  }
  state.finished = true;
  await setProgress(state);
  return state;
}

function sendToTab(tabId, message) {
  return new Promise((resolve, reject) => {
    chrome.tabs.sendMessage(tabId, message, (resp) => {
      if (chrome.runtime.lastError) { reject(new Error(chrome.runtime.lastError.message)); return; }
      resolve(resp);
    });
  });
}

async function pingTab(tabId) {
  try { await sendToTab(tabId, { type: 'PING' }); return true; } catch (_) { return false; }
}

// content_scripts declared in manifest.json only auto-inject on page
// load/navigation - a tab that was already open before the extension was
// installed/reloaded has no content script alive to answer messages, which
// is what "Receiving end does not exist" means. chrome.scripting.executeScript
// re-injects it into the tab as it already is, no page reload needed.
async function ensureContentScript(tabId) {
  if (await pingTab(tabId)) return true;
  try {
    await chrome.scripting.executeScript({ target: { tabId }, files: ['content.js'] });
  } catch (e) {
    console.warn(TAG, 'content.js injection failed:', e?.message);
    return false;
  }
  return pingTab(tabId);
}

// One retry, only for the specific "nothing answered" failure classes (dead
// content script, or a message port that closed before a response arrived) -
// any other error (e.g. Amazon's API itself returning an error) is rethrown
// immediately since retrying it would just repeat the same real failure.
async function sendToTabWithRecovery(tabId, message) {
  try {
    return await sendToTab(tabId, message);
  } catch (e) {
    if (!/Receiving end does not exist|Could not establish connection|message port closed/i.test(e.message)) throw e;
    console.warn(TAG, 'relay failed, attempting recovery:', e.message);
    if (!(await ensureContentScript(tabId))) throw e;
    return sendToTab(tabId, message);
  }
}

// ── Navigation orchestration (Bulk only) ────────────────────────────────────
// Standard Returns and FBA Returns are two separate Seller Central pages
// (different URLs), each with its own CSRF context that only exists once
// that specific page has loaded - opening one does NOT initialize the other.
// Every other module (Orders/Payments/GST) has no such requirement and
// keeps working from whatever sellercentral tab is already open, unchanged.
//
// Report modules stay unaware of any of this - they still just take a tabId
// and do schedule/poll/download. This registry plus the resolve/navigate
// helpers below (used only from bulkSyncTick) are what decide *which* tabId
// to hand them, opening/reusing/navigating a page on their behalf first.
const PAGE_REQUIREMENTS = {
  // Confirmed live (Return Reports page, screenshot) - the `ref=xx_...`
  // suffix Amazon's own left-nav adds is just an analytics tracking param,
  // not required for the page itself to load and initialize correctly.
  returns: {
    label: 'Standard Returns',
    url: 'https://sellercentral.amazon.in/returns/report',
    readyCheck: 'RETURNS_PAGE_READY',
    neededFor: ['schedule'], // poll/download are CSRF-free - confirmed, no page needed for those
  },
  // Confirmed live (screenshot, 27 Jul 2026): Fulfilment Reports -> FBA
  // customer returns settles at this fixed path. Like `returns` above,
  // sellercentral.amazon.in pages are session/cookie-scoped rather than
  // parameterized by account in the URL (unlike Ads' entityId), so this one
  // URL should work for any seller/tenant without ever needing to visit the
  // page first. cachePageUrl-learned URLs (see resolvePageForCategory) are
  // now a fallback behind this, kept in case a future Seller Central
  // redesign or marketplace variant ever moves this path.
  fbaReturns: {
    label: 'FBA Customer Returns',
    url: 'https://sellercentral.amazon.in/reportcentral/CUSTOMER_RETURNS/1',
    readyCheck: 'FBA_RETURNS_PAGE_READY',
    neededFor: ['schedule', 'poll'], // both need the page - confirmed, both throw csrfMissing
  },
  // A different origin from every other requirement here (advertising.amazon.in,
  // not sellercentral.amazon.in) - originPattern lets findReadyTabForRequirement
  // search the right one. Confirmed live: navigating straight to this bare
  // URL (no entityId) auto-redirects to the seller's own
  // https://.../reports?entityId=... - same "fixed URL, no learning needed"
  // shape as `returns` above, not `fbaReturns`'s learned-URL shape.
  // cachePageUrl('ads', ...) calls elsewhere stay harmless no-ops (same as
  // `returns`'s own redundant caching), kept for consistency.
  ads: {
    label: 'Ads Reports',
    url: 'https://advertising.amazon.in/reports',
    originPattern: 'https://advertising.amazon.in/*',
    readyCheck: 'ADS_PAGE_READY',
    neededFor: ['schedule'],
  },
};

async function getCachedPageUrl(cat) {
  const { pageUrlCache } = await chrome.storage.local.get('pageUrlCache');
  return pageUrlCache?.[cat] || null;
}
async function cachePageUrl(cat, url) {
  if (!url || !PAGE_REQUIREMENTS[cat]) return;
  const { pageUrlCache } = await chrome.storage.local.get('pageUrlCache');
  if (pageUrlCache?.[cat] === url) return;
  await chrome.storage.local.set({ pageUrlCache: { ...(pageUrlCache || {}), [cat]: url } });
}

async function checkPageReady(tabId, requirement) {
  try {
    const resp = await chrome.tabs.sendMessage(tabId, { type: requirement.readyCheck });
    return !!resp?.ready;
  } catch (_) {
    return false;
  }
}

// Covers the case where the user already has the right page open themselves
// (in any tab) - reusing it means no navigation/new tab is needed at all.
async function findReadyTabForRequirement(requirement) {
  const tabs = await chrome.tabs.query({ url: requirement.originPattern || 'https://sellercentral.amazon.in/*' });
  for (const tab of tabs) {
    if (await checkPageReady(tab.id, requirement)) return tab.id;
  }
  return null;
}

function waitForTabComplete(tabId, timeoutMs = 20000) {
  return new Promise((resolve, reject) => {
    const timer = setTimeout(() => { cleanup(); reject(new Error('Timed out waiting for the page to load.')); }, timeoutMs);
    function listener(id, info) {
      if (id === tabId && info.status === 'complete') { cleanup(); resolve(); }
    }
    function cleanup() { clearTimeout(timer); chrome.tabs.onUpdated.removeListener(listener); }
    chrome.tabs.onUpdated.addListener(listener);
  });
}

// Polls (rather than guessing a fixed delay) because page-initialization
// time genuinely varies - this is the same content-script readiness signal
// each module already reads its CSRF token from, just exposed as a boolean.
async function pollPageReady(tabId, requirement, timeoutMs = 20000) {
  const deadline = Date.now() + timeoutMs;
  while (Date.now() < deadline) {
    if (await checkPageReady(tabId, requirement)) return true;
    await sleep(500);
  }
  return false;
}

async function navigateAndPreparePage(tabId, requirement, url) {
  await chrome.tabs.update(tabId, { url });
  await waitForTabComplete(tabId);
  if (!(await ensureContentScript(tabId))) throw new Error(`Could not connect to the ${requirement.label} page after opening it. Please try again.`);
  const ready = await pollPageReady(tabId, requirement);
  if (!ready) throw new Error(`Could not confirm the ${requirement.label} page finished initializing. Try again, or open it manually first.`);
}

// The single entry point bulkSyncTick calls before any page-bound call.
// Returns the tabId to actually use for that call (which may be job.tabId
// unchanged, an existing tab the user already has the right page open in,
// or a shared temporary tab this function opens/navigates on the run's
// behalf) - `job` is mutated in place with any nav-tab bookkeeping the
// caller needs to persist, since this all has to survive an MV3 service
// worker restart mid-run just like every other piece of job state here.
async function resolvePageForCategory(job, cat, step) {
  const requirement = PAGE_REQUIREMENTS[cat];
  if (!requirement || !requirement.neededFor.includes(step)) return job.tabId;

  // Already navigated to this exact requirement earlier in the same run -
  // re-verify it's still ready rather than re-navigating for nothing.
  if (job.navTabId && job.navTabFor === cat && (await checkPageReady(job.navTabId, requirement))) {
    return job.navTabId;
  }

  // Covers the case where the user already has the right page open
  // themselves (in any tab) - checked before any URL is even needed, so it
  // also catches this on a first-ever run before anything has been learned.
  const existing = await findReadyTabForRequirement(requirement);
  if (existing) {
    await releaseNavTab(job); // a temp tab opened for a *previous* category is no longer needed
    // Don't trust it as-is. checkPageReady only confirms *some* CSRF token
    // is present in that tab's DOM - not that it's current. A tab that's
    // simply been sitting open still carries whatever seller account was
    // selected the last time IT actually loaded a page: Amazon's account
    // switcher updates cookies globally the instant you switch, but only a
    // page that (re)loads afterward picks that up. Silently reusing this
    // tab as-is was the actual cause of syncs pulling the previous seller
    // account's data with no error after switching accounts without a
    // manual page refresh - this forced reload IS that refresh, done for
    // you, before any report call is ever made against the tab.
    const url = requirement.url || (await getCachedPageUrl(cat)) || (await chrome.tabs.get(existing)).url;
    await navigateAndPreparePage(existing, requirement, url);
    return existing;
  }

  // Try the confirmed/hardcoded URL first, then whatever was previously
  // learned for this account via cachePageUrl - a fallback for if the
  // hardcoded one ever stops working (a Seller Central redesign, a
  // marketplace variant, etc). Only if every candidate fails to load do we
  // fall back to job.tabId, whose own csrfMissing error asks the seller to
  // open the page once manually - same safety net as before, just now a
  // last resort instead of the common case.
  const cachedUrl = await getCachedPageUrl(cat);
  const candidates = [...new Set([requirement.url, cachedUrl].filter(Boolean))];
  if (!candidates.length) return job.tabId;

  for (const url of candidates) {
    if (!job.navTabId) {
      const tab = await chrome.tabs.create({ url, active: false });
      job.navTabId = tab.id;
      job.navTabOwned = true;
    } else {
      await chrome.tabs.update(job.navTabId, { url });
    }
    job.navTabFor = cat;
    try {
      await navigateAndPreparePage(job.navTabId, requirement, url);
      return job.navTabId;
    } catch (_) {
      // try the next candidate, if any
    }
  }
  return job.tabId;
}

// Only closes tabs this run opened itself - a tab the user already had open
// (found via findReadyTabForRequirement) is never touched.
async function releaseNavTab(job) {
  if (job.navTabId && job.navTabOwned) {
    try { await chrome.tabs.remove(job.navTabId); } catch (_) {}
  }
  job.navTabId = null;
  job.navTabOwned = false;
  job.navTabFor = null;
}

// Ads' own single-tab job (like Orders/Payments/GST) only needs to resolve a
// tab once at sync start, not per-category like Bulk - so this is a smaller
// standalone version of resolvePageForCategory's same three-tier strategy
// (already-open tab -> cached/learned URL -> give up and ask the user to
// open it once), not routed through Bulk's job.navTabId bookkeeping.
async function resolveAdsTab() {
  const requirement = PAGE_REQUIREMENTS.ads;
  const existing = await findReadyTabForRequirement(requirement);
  if (existing) {
    // Same reasoning as resolvePageForCategory's identical check: don't trust
    // an already-open tab as-is just because it has *some* CSRF token -
    // force a fresh navigation so it reflects whichever seller account is
    // currently selected, not whichever one was active the last time this
    // specific tab happened to load.
    const url = requirement.url || (await getCachedPageUrl('ads')) || (await chrome.tabs.get(existing)).url;
    await navigateAndPreparePage(existing, requirement, url);
    return { tabId: existing, opened: false };
  }

  const url = requirement.url || (await getCachedPageUrl('ads'));
  if (!url) return { tabId: null, opened: false };

  const tab = await chrome.tabs.create({ url, active: false });
  try {
    await navigateAndPreparePage(tab.id, requirement, url);
  } catch (e) {
    // Unlike resolvePageForCategory (which mutates a job object the caller
    // already owns, so a later releaseNavTab can still find and close it
    // even mid-throw), this tab was just created and nothing else knows
    // about it yet - clean it up here instead of leaving it orphaned.
    try { await chrome.tabs.remove(tab.id); } catch (_) {}
    throw e;
  }
  return { tabId: tab.id, opened: true };
}

// Only closes the tab this run opened itself (mirrors releaseNavTab) - a tab
// the user already had open (resolveAdsTab's `opened: false` case) is never
// touched.
async function releaseAdsTab(job) {
  if (job?.navTabId && job?.navTabOwned) {
    try { await chrome.tabs.remove(job.navTabId); } catch (_) {}
  }
}

// Sanitizes a single name component (the tenant/business name) the same way
// Meesho's safeName() does, so the two marketplaces' ZIP filenames follow one
// consistent pattern: "<Marketplace>_<Type>_<Shop>_<from>_to_<to>.zip".
function safeName(s) {
  return (
    String(s || 'Seller')
      .replace(/[^a-z0-9]/gi, '_')
      .replace(/_+/g, '_')
      .replace(/^_|_$/g, '') || 'Seller'
  );
}

// The Speed Ecom account's registered business/company name (shown in the
// account bar) - fallback only now (see fetchSellerAccountName below), used
// when the live Amazon account name can't be scraped (different origin like
// advertising.amazon.in, dead tab, page structure changed, etc).
async function getTenantName() {
  let auth;
  try {
    ({ auth } = await chrome.storage.local.get('auth'));
  } catch (_) {
    return 'Seller';
  }
  return safeName(auth?.user?.tenant || auth?.user?.name || 'Seller');
}

// A SpeedEcom tenant can run more than one Amazon Seller Central account, so
// the ZIP name needs to reflect whichever account was actually open at sync
// time, not the (single, tenant-wide) SpeedEcom account name - otherwise
// ZIPs from different Amazon accounts under the same tenant are
// indistinguishable by filename. `null` (not a thrown error) means "couldn't
// scrape it" - a dead tab, a different origin (Ads), or the page's markup
// having changed - so the caller can fall back to the tenant name instead of
// failing the whole zip.
async function fetchSellerAccountName(tabId) {
  try {
    const resp = await sendToTabWithRecovery(tabId, { type: 'GET_SELLER_ACCOUNT_NAME' });
    return resp?.name || null;
  } catch (_) {
    return null;
  }
}

// Builds the outer ZIP's filename. No more user-editable "Zip File Name"
// field - it's always derived from the shop name and the requested date
// range, same idea as Meesho's Meesho_<Shop>_<from>_to_<to>.zip. `label`
// distinguishes Amazon's several report types (Orders/Payments/GST/...),
// which Meesho doesn't need since it only has one sync flow.
async function buildZipName(label, fromDate, toDate, isRetry, tabId) {
  const scraped = tabId ? await fetchSellerAccountName(tabId) : null;
  const tenant = scraped ? safeName(scraped) : await getTenantName();
  const base = `Amazon_${label}_${tenant}_${fromDate}_to_${toDate}`;
  return isRetry ? `${base}_Retry` : base;
}

function buildOrderFilename(fromDate, toDate, ext) {
  return fromDate === toDate ? `Orders_${fromDate}${ext}` : `Orders_${fromDate}_to_${toDate}${ext}`;
}

// ISO (YYYY-MM-DD, what the date picker gives us) -> the two different
// formats Amazon's Returns UI actually uses: MM/DD/YYYY for the generate
// request body (confirmed live: fromDate=06/01/2026&toDate=07/14/2026), and
// DD-Mon-YYYY for matching against the list's displayed "Date range covered"
// text (e.g. "01-Jun-2026").
function isoToUS(iso) {
  const [y, m, d] = iso.split('-');
  return `${m}/${d}/${y}`;
}
const RETURNS_MONTH_ABBR = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
function isoToDisplayDate(iso) {
  const [y, m, d] = iso.split('-').map(Number);
  return `${String(d).padStart(2, '0')}-${RETURNS_MONTH_ABBR[m - 1]}-${y}`;
}

function buildReturnsFilename(fromDate, toDate, ext) {
  return fromDate === toDate ? `Returns_${fromDate}${ext}` : `Returns_${fromDate}_to_${toDate}${ext}`;
}

function buildPaymentsFilename(fromDate, toDate, ext) {
  return fromDate === toDate ? `Payments_${fromDate}${ext}` : `Payments_${fromDate}_to_${toDate}${ext}`;
}

function buildGstFilename(label, fromDate, toDate, ext) {
  return fromDate === toDate ? `GST_${label}_${fromDate}${ext}` : `GST_${label}_${fromDate}_to_${toDate}${ext}`;
}

function buildFbaReturnsFilename(fromDate, toDate, ext) {
  return fromDate === toDate ? `FBA_Returns_${fromDate}${ext}` : `FBA_Returns_${fromDate}_to_${toDate}${ext}`;
}

// ISO (YYYY-MM-DD) -> Report Central's YYYY/MM/DD - confirmed live from a
// captured submitDownloadReport request (reportStartDate=2025%2F05%2F01).
function isoToSlashYMD(iso) {
  return iso.replace(/-/g, '/');
}


// ── Orders report engine ────────────────────────────────────────────────────
// Confirmed live: All Orders -> Order Date -> Exact dates -> Request. No
// per-report cooldown was observed here (unlike Myntra's real 15-minute one) -
// several back-to-back manual requests all succeeded immediately - so unlike
// Myntra, this always schedules a fresh report rather than trying to detect
// and reuse an existing one. That reuse-detection would also be fragile here:
// the status list reformats dates into a locale string ("01/07/26, 12:00:00
// am India Standard Time") that doesn't round-trip cleanly back to the
// ISO date this extension sends, so matching by exact string equality
// would be more likely to silently miss real matches than to save anything.

async function scheduleOrderReport(tabId, { startDate, endDate }) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'SCHEDULE_ORDER_REPORT', startDate, endDate });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not schedule the Order Report.');
  if (resp.status >= 400) {
    throw new Error(resp.body?.message || resp.body?.status?.statusMessage || `Amazon rejected the request (HTTP ${resp.status}).`);
  }
  const referenceId = resp.body?.referenceId;
  if (!referenceId) throw new Error('Report scheduling did not return a reference id.');
  return referenceId;
}

async function fetchOrderStatusList(tabId) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'FETCH_ORDER_STATUS' });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not read the Order Report status list.');
  if (resp.status >= 400) throw new Error(`Amazon returned HTTP ${resp.status} for the report status list.`);
  return resp.body?.reportRequestResult || [];
}

// One tick == one short, self-contained unit of work, always safe to be the
// last thing that ever runs before the worker gets reclaimed - nothing here
// depends on any state that isn't already sitting in chrome.storage.local by
// the time the tick returns. tickInFlight only guards against two ticks
// (a scheduled one and an immediate Pause/Cancel-triggered one) racing each
// other within the SAME live worker instance - it's fine for this to reset
// to false on a restart, since a restart already means no tick was actually
// running anymore.
let tickInFlight = false;
async function orderSyncTick() {
  if (tickInFlight) return;
  tickInFlight = true;
  try {
    const job = await getOrderJob();
    if (!job) { await chrome.alarms.clear(TICK_ALARM_NAME); return; }

    if (job.cancelled) {
      await clearOrderJob();
      await setProgress(newState({ finished: true, cancelled: true, current: 'Cancelled - no files were downloaded yet.' }));
      return;
    }
    if (job.paused) {
      await setProgress(newState({ current: 'Paused', paused: true }));
      return;
    }

    if (job.phase === 'scheduling') {
      await setProgress(newState({ current: 'Requesting Order Report...' }));
      const referenceId = await scheduleOrderReport(job.tabId, { startDate: job.fromDate, endDate: job.toDate });
      await setOrderJob({ ...job, phase: 'polling', referenceId });
      await setProgress(newState({ current: 'Waiting for Amazon to generate the Order Report (this can take up to 45 minutes)...' }));
      return;
    }

    if (job.phase === 'polling') {
      const jobs = await fetchOrderStatusList(job.tabId);
      const found = jobs.find((j) => j.referenceId === job.referenceId);
      if (found?.stateReady) {
        await setOrderJob({ ...job, phase: 'downloading' });
        return; // next tick actually downloads - keeps every tick short
      }
      if (found?.stateNoData) {
        await clearOrderJob();
        await setProgress(newState({ finished: true, error: 'No orders were found for that date range.' }));
        return;
      }
      if (found?.requestStateName && /FAIL|ERROR/i.test(found.requestStateName)) {
        throw new Error(found.requestStateName);
      }
      const waitedMs = Date.now() - job.startedAt;
      if (waitedMs > 50 * 60 * 1000) { // Amazon's own UI says "up to 45 minutes" - pad a little
        throw new Error('The Order Report did not finish generating within the expected time. Try again shortly.');
      }
      await setProgress(newState({ current: 'Waiting for Amazon to generate the Order Report (this can take up to 45 minutes)...' }));
      return;
    }

    if (job.phase === 'downloading') {
      const filename = buildOrderFilename(job.fromDate, job.toDate, '.txt');
      const tasks = [{ referenceId: job.referenceId, filename, zipFolder: 'Orders' }];
      const state = await processDownloadQueue(tasks, await buildZipName('Orders', job.fromDate, job.toDate, undefined, job.tabId), job.tabId);
      await recordLastSync('Orders', state);
      await clearOrderJob();
      return;
    }
  } catch (e) {
    console.warn(TAG, 'sync failed:', e?.message);
    await clearOrderJob();
    await setProgress(newState({ finished: true, error: e?.message || 'Sync failed.' }));
  } finally {
    tickInFlight = false;
  }
}

// Registered unconditionally, at the top level, every time this script
// executes - which is exactly what makes it survive a worker restart. If
// this were only registered from inside some conditionally-run function
// (e.g. only when a sync starts), a restart triggered while a sync was
// mid-flight would come back up with nothing listening for the alarm at all.
chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === TICK_ALARM_NAME) {
    orderSyncTick().catch((e) => console.error(TAG, 'tick failed:', e?.message));
  }
});

async function startOrderSync({ fromDate, toDate, tabId }) {
  await setOrderJob({
    phase: 'scheduling',
    fromDate, toDate, tabId,
    referenceId: null,
    paused: false,
    cancelled: false,
    startedAt: Date.now(),
  });
  await setProgress(newState({ current: 'Starting sync...' }));
  chrome.alarms.create(TICK_ALARM_NAME, { periodInMinutes: TICK_INTERVAL_MINUTES });
  // Run the first tick immediately rather than waiting up to 30s for the
  // first alarm to fire - the popup should see real progress right away.
  await orderSyncTick();
}

// ── Returns report engine ───────────────────────────────────────────────────
// Confirmed live: All Returns -> Exact dates -> Request, TSV only (XML
// skipped for now - Speed Ecom's own Uploads page doesn't accept .xml).
// Two real differences from Orders that shape this whole engine:
//
// 1. generate needs a csrfToken read fresh from the page's own DOM
//    (#returns-report-csrf-token) - confirmed after exhausting every other
//    static source (DOM search, page source, loaded JS, cookies, storage,
//    network bodies) that it only ever exists there, read by Amazon's own
//    requestAdhocReport function via `b("#returns-report-csrf-token").val()`.
//    That element only exists on the actual Return Reports page, so this
//    requires the active Seller Central tab to be on it.
//
// 2. generate's response carries no report id at all (just an HTML
//    "SUCCESS" page) - so unlike Orders, there's no id to poll for
//    directly. Instead, the list is snapshotted for matching (type +
//    date-range) entries BEFORE calling generate, and polling looks for a
//    new matching entry whose "Requested On" text wasn't in that snapshot -
//    a set-difference instead of a returned id or a parsed timestamp,
//    sidestepping any IST/local-timezone parsing entirely.

async function scheduleReturnsReport(tabId, { fromDate, toDate }) {
  const resp = await sendToTabWithRecovery(tabId, {
    type: 'SCHEDULE_RETURNS_REPORT', fromDate: isoToUS(fromDate), toDate: isoToUS(toDate),
  });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not request the Returns report.');
  if (resp.csrfMissing) throw new Error("Open Seller Central's Return Reports page (Returns → Return Reports) in this tab, then try again.");
  if (resp.status >= 400) throw new Error(`Amazon rejected the request (HTTP ${resp.status}).`);
  await cachePageUrl('returns', resp.pageUrl);
}

async function fetchReturnsListRecords(tabId) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'FETCH_RETURNS_LIST' });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not read the Returns report list.');
  if (resp.status >= 400) throw new Error(`Amazon returned HTTP ${resp.status} for the returns list.`);
  return resp.records || [];
}

function matchingReturnsRecords(records, fromDate, toDate) {
  const start = isoToDisplayDate(fromDate);
  const end = isoToDisplayDate(toDate);
  return records.filter((r) => r.type === 'All Returns' && r.dateRangeStart === start && r.dateRangeEnd === end);
}

// The individual Returns tab's own sync runs through the Bulk engine now
// (see startBulkSync/bulkSyncTick below), scoped to categories drawn from
// ['returns', 'fbaReturns'] - that engine already generalizes to any subset
// of categories and already carries the navigation orchestration both of
// these need, so a separate single-purpose tick/job/alarm here would just
// be a second, divergent implementation of the exact same logic.

// ── Payments report engine ──────────────────────────────────────────────────
// Confirmed live: Account Type "All (Unified Reports)" -> Report Type
// "Transaction" -> Custom Date Range -> Request Report. The cleanest of the
// three integrations - plain JSON, no CSRF token, and the schedule response
// hands back a real reportId directly (like Orders), so this polls that one
// id via a dedicated per-report status endpoint rather than a list. Also
// confirmed live: no enforced max date-range width here (an 18+ month
// request was accepted and processed, unlike Orders' 30-day / Returns'
// 60-day caps) - so no client-side range cap is applied, only the same
// 2-days-back cutoff used everywhere else for consistency.

async function schedulePaymentsReport(tabId, { fromDate, toDate }) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'SCHEDULE_PAYMENTS_REPORT', fromDate, toDate });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not schedule the Payments report.');
  if (resp.status >= 400) throw new Error(resp.body?.message || `Amazon rejected the request (HTTP ${resp.status}).`);
  const reportId = resp.body?.reportId || resp.body?.generatedReport?.reportId;
  if (!reportId) throw new Error('Report scheduling did not return a report id.');
  return reportId;
}

async function fetchPaymentsReportStatus(tabId, reportId) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'FETCH_PAYMENTS_STATUS', reportId });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not read the Payments report status.');
  if (resp.status >= 400) throw new Error(`Amazon returned HTTP ${resp.status} for the report status.`);
  return resp.body;
}

let tickInFlightPayments = false;
async function paymentsSyncTick() {
  if (tickInFlightPayments) return;
  tickInFlightPayments = true;
  try {
    const job = await getPaymentsJob();
    if (!job) { await chrome.alarms.clear(PAYMENTS_TICK_ALARM_NAME); return; }

    if (job.cancelled) {
      await clearPaymentsJob();
      await setProgress(newState({ finished: true, cancelled: true, current: 'Cancelled - no files were downloaded yet.' }));
      return;
    }
    if (job.paused) {
      await setProgress(newState({ current: 'Paused', paused: true }));
      return;
    }

    if (job.phase === 'scheduling') {
      await setProgress(newState({ current: 'Requesting Payments report...' }));
      const reportId = await schedulePaymentsReport(job.tabId, { fromDate: job.fromDate, toDate: job.toDate });
      await setPaymentsJob({ ...job, phase: 'polling', reportId });
      await setProgress(newState({ current: 'Waiting for Amazon to generate the Payments report...' }));
      return;
    }

    if (job.phase === 'polling') {
      const status = await fetchPaymentsReportStatus(job.tabId, job.reportId);
      if (status?.status === 'DOWNLOADABLE') {
        await setPaymentsJob({ ...job, phase: 'downloading' });
        return; // next tick actually downloads - keeps every tick short
      }
      if (status?.status && /CANCEL|FAIL|ERROR/i.test(status.status)) {
        throw new Error(`Payments report ${status.status.toLowerCase()}. Try again.`);
      }
      const waitedMs = Date.now() - job.startedAt;
      if (waitedMs > 50 * 60 * 1000) {
        throw new Error('The Payments report did not finish generating within the expected time. Try again shortly.');
      }
      await setProgress(newState({ current: 'Waiting for Amazon to generate the Payments report...' }));
      return;
    }

    if (job.phase === 'downloading') {
      const filename = buildPaymentsFilename(job.fromDate, job.toDate, '.csv');
      const tasks = [{ reportId: job.reportId, filename, zipFolder: 'Payments', kind: 'payments' }];
      const state = await processDownloadQueue(tasks, await buildZipName('Payments', job.fromDate, job.toDate, undefined, job.tabId), job.tabId);
      await recordLastSync('Payments', state);
      await clearPaymentsJob();
      return;
    }
  } catch (e) {
    console.warn(TAG, 'payments sync failed:', e?.message);
    await clearPaymentsJob();
    await setProgress(newState({ finished: true, error: e?.message || 'Payments sync failed.' }));
  } finally {
    tickInFlightPayments = false;
  }
}

chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === PAYMENTS_TICK_ALARM_NAME) {
    paymentsSyncTick().catch((e) => console.error(TAG, 'payments tick failed:', e?.message));
  }
});

async function startPaymentsSync({ fromDate, toDate, tabId }) {
  await setPaymentsJob({
    phase: 'scheduling',
    fromDate, toDate, tabId,
    paused: false,
    cancelled: false,
    startedAt: Date.now(),
  });
  await setProgress(newState({ current: 'Starting sync...' }));
  chrome.alarms.create(PAYMENTS_TICK_ALARM_NAME, { periodInMinutes: TICK_INTERVAL_MINUTES });
  await paymentsSyncTick();
}

// ── GST On Demand Reports engine (B2B/B2C) ─────────────────────────────────
// Confirmed live: GST Reporting -> GST On Demand Reports -> Merchant Tax
// Report (MTR, always selected - Stock Transfer Report is out of scope) ->
// B2B or B2C radio -> Exact Dates -> Request Report. reportType is the real
// UI's own numeric code for that radio: "59300" for B2B, "61200" for B2C.
//
// Two things shape this engine, both mirroring Returns:
// 1. create-report's response is a bare 204 - no report id at all - so like
//    Returns, the report-history list is snapshotted (by dateRequested,
//    scoped to this reportType) before scheduling, and polling looks for a
//    new matching entry not in that snapshot.
// 2. The eventual download needs three fields read verbatim off the matched
//    report-history entry once its reportStatus is "Done": reportDocumentId,
//    dateRangeCovered (joined "start - end", exactly as displayed), and
//    reportType - confirmed live via fetch/XHR/anchor-click interception,
//    since create-report never returns a document id itself.
//
// B2B and B2C are just two different reportType values against the same
// create/list endpoints - Amazon's report-history list already returns both
// side by side, distinguished only by reportType. So both can be requested
// in the same run: `types` holds whichever of ['B2B','B2C'] the user picked,
// and `sub` tracks each one's own phase independently (scheduling loop
// mirrors Bulk's per-category loop, just scoped to at most these two types).
const GST_TYPE_REPORT_TYPE = { B2B: '59300', B2C: '61200' };

async function scheduleGstReport(tabId, { reportType, fromDate, toDate }) {
  const startDate = new Date(`${fromDate}T00:00:00+05:30`).getTime();
  const endDate = new Date(`${toDate}T23:59:59.999+05:30`).getTime();
  const resp = await sendToTabWithRecovery(tabId, { type: 'SCHEDULE_GST_REPORT', reportType, startDate, endDate });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not request the GST report.');
  if (resp.status >= 400) throw new Error(`Amazon rejected the request (HTTP ${resp.status}).`);
}

async function fetchGstReportList(tabId) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'FETCH_GST_LIST' });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not read the GST report list.');
  if (resp.status >= 400) throw new Error(`Amazon returned HTTP ${resp.status} for the GST report list.`);
  return resp.body?.reportMetadataList || [];
}

function matchingGstRecords(records, reportType) {
  return records.filter((r) => String(r.reportType) === String(reportType));
}

// ── GST Monthly Reports (auto-selected fallback for out-of-range dates) ────
// GST On Demand only covers the last 45 days (Amazon's own limit). Per
// direct instruction, this is picked automatically per whether the picked
// range fits inside that window - never exposed as a separate choice in the
// UI. Confirmed live: GST Monthly Reports live under this exact same
// /fba/gstreports/* app, pre-generated by Amazon (available the 5th of the
// following month) rather than scheduled on request, so there's no
// create/poll step at all - monthly-metadata's own response already comes
// back reportStatus:"Done" (or every field null if that month has no
// report). reportType codes are Monthly's own, confirmed live MTR B2B =
// 41200, MTR B2C = 39400 (different from On Demand's 59300/61200).
const GST_MONTHLY_REPORT_TYPE = { B2B: '41200', B2C: '39400' };
const GST_MONTHLY_LOOKBACK_DAYS = 45; // matches On Demand's own confirmed 45-day window
const GST_MONTH_NAMES = ['JANUARY', 'FEBRUARY', 'MARCH', 'APRIL', 'MAY', 'JUNE', 'JULY', 'AUGUST', 'SEPTEMBER', 'OCTOBER', 'NOVEMBER', 'DECEMBER'];

function gstMonthlyCutoffIso() {
  const cutoff = new Date();
  cutoff.setDate(cutoff.getDate() - (GST_MONTHLY_LOOKBACK_DAYS - 1));
  const pad = (v) => String(v).padStart(2, '0');
  return `${cutoff.getFullYear()}-${pad(cutoff.getMonth() + 1)}-${pad(cutoff.getDate())}`;
}

// Whole-range decision, not per-day - if any part of the picked range falls
// outside the 45-day On Demand window, the entire range is handled via
// Monthly instead (auto-split per whole calendar month it overlaps), per
// direct instruction rather than trying to blend both sources for one run.
function gstRangeNeedsMonthly(fromDate) {
  return fromDate < gstMonthlyCutoffIso();
}

function gstMonthsInRange(fromDate, toDate) {
  const start = new Date(`${fromDate}T00:00:00`);
  const end = new Date(`${toDate}T00:00:00`);
  const months = [];
  let cursor = new Date(start.getFullYear(), start.getMonth(), 1);
  while (cursor <= end) {
    months.push({ month: GST_MONTH_NAMES[cursor.getMonth()], year: cursor.getFullYear() });
    cursor = new Date(cursor.getFullYear(), cursor.getMonth() + 1, 1);
  }
  return months;
}

async function fetchMonthlyGstMetadata(tabId, { reportType, month, year }) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'FETCH_MONTHLY_GST_METADATA', reportType, month, year });
  if (!resp?.ok) throw new Error(resp?.error || `Could not check the ${month} ${year} GST Monthly report.`);
  if (resp.status >= 400) throw new Error(`Amazon returned HTTP ${resp.status} for the ${month} ${year} GST Monthly report.`);
  return resp.body || {};
}

// Resolves every whole month the range overlaps, synchronously (no polling
// needed - these reports are already pre-generated or they simply don't
// exist yet). A month with no report available is silently skipped rather
// than failing the whole category - only if NONE of the months have a
// report does this surface as a failure.
async function resolveGstMonthlyDownloads(tabId, { label, fromDate, toDate }) {
  const reportType = GST_MONTHLY_REPORT_TYPE[label];
  const months = gstMonthsInRange(fromDate, toDate);
  const downloads = [];
  for (const { month, year } of months) {
    const body = await fetchMonthlyGstMetadata(tabId, { reportType, month, year });
    if (!body.documentId || body.reportStatus !== 'Done') continue;
    downloads.push({ documentId: body.documentId, reportType, monthLabel: `${month}_${year}` });
  }
  return downloads;
}

function gstStatusText(types, sub) {
  return types.map((t) => {
    const phase = sub[t]?.phase;
    if (phase === 'ready') return `${t}: ready`;
    if (phase === 'empty') return `${t}: no data`;
    if (phase === 'failed') return `${t}: failed`;
    if (phase === 'scheduling') return `${t}: requesting...`;
    return `${t}: waiting...`;
  }).join('  ·  ');
}

let tickInFlightGst = false;
async function gstSyncTick() {
  if (tickInFlightGst) return;
  tickInFlightGst = true;
  try {
    const job = await getGstJob();
    if (!job) { await chrome.alarms.clear(GST_TICK_ALARM_NAME); return; }

    if (job.cancelled) {
      await clearGstJob();
      await setProgress(newState({ finished: true, cancelled: true, current: 'Cancelled - no files were downloaded yet.' }));
      return;
    }
    if (job.paused) {
      await setProgress(newState({ current: 'Paused', paused: true, gst: { types: job.types, sub: job.sub } }));
      return;
    }

    const { types } = job;
    const sub = { ...job.sub };

    if (job.phase === 'scheduling') {
      for (const t of types) {
        if (sub[t].phase !== 'scheduling') continue;
        try {
          if (gstRangeNeedsMonthly(job.fromDate)) {
            // Out of On Demand's 45-day window - resolved synchronously
            // right here (Monthly reports are pre-generated, nothing to
            // poll for), so this jumps straight to 'ready'/'failed',
            // skipping the 'polling' phase entirely for this type.
            const downloads = await resolveGstMonthlyDownloads(job.tabId, { label: t, fromDate: job.fromDate, toDate: job.toDate });
            if (!downloads.length) sub[t] = { phase: 'empty', error: 'No GST Monthly reports were available for that date range.' };
            else sub[t] = { phase: 'ready', downloads };
          } else {
            const reportType = GST_TYPE_REPORT_TYPE[t];
            const before = await fetchGstReportList(job.tabId);
            const beforeSet = matchingGstRecords(before, reportType).map((r) => r.dateRequested);
            await scheduleGstReport(job.tabId, { reportType, fromDate: job.fromDate, toDate: job.toDate });
            sub[t] = { phase: 'polling', beforeSet };
          }
        } catch (e) {
          sub[t] = { phase: 'failed', error: e?.message || 'Could not request this report.' };
        }
      }
      await setGstJob({ ...job, sub, phase: 'polling' });
      await setProgress(newState({ current: gstStatusText(types, sub), gst: { types, sub } }));
      return;
    }

    if (job.phase === 'polling') {
      const waitedMs = Date.now() - job.startedAt;
      for (const t of types) {
        if (sub[t].phase !== 'polling') continue;
        try {
          const reportType = GST_TYPE_REPORT_TYPE[t];
          const records = await fetchGstReportList(job.tabId);
          const candidates = matchingGstRecords(records, reportType).filter((r) => !sub[t].beforeSet.includes(r.dateRequested));
          if (candidates.length) {
            const record = candidates[0];
            if (record.reportStatus === 'Done') {
              if (!record.reportDocumentId) sub[t] = { phase: 'empty', error: 'No records were found for that date range.' };
              else sub[t] = { ...sub[t], phase: 'ready', documentId: record.reportDocumentId, dateRangeCovered: (record.dateRangeCovered || []).join(' - ') };
            } else if (record.reportStatus && /FAIL|ERROR/i.test(record.reportStatus)) {
              sub[t] = { phase: 'failed', error: `GST report ${record.reportStatus.toLowerCase()}.` };
            }
          }
        } catch (e) {
          sub[t] = { phase: 'failed', error: e?.message || 'Could not read this report\'s status.' };
        }
        if (sub[t].phase === 'polling' && waitedMs > 50 * 60 * 1000) {
          sub[t] = { phase: 'failed', error: 'Did not finish generating within the expected time.' };
        }
      }

      const stillPolling = types.some((t) => sub[t].phase === 'polling');
      if (!stillPolling) {
        await setGstJob({ ...job, sub, phase: 'downloading' });
        return;
      }
      await setGstJob({ ...job, sub });
      await setProgress(newState({ current: gstStatusText(types, sub), gst: { types, sub } }));
      return;
    }

    if (job.phase === 'downloading') {
      const tasks = [];
      const failures = [];
      const noData = [];
      for (const t of types) {
        const s = sub[t];
        if (s.phase === 'ready' && s.downloads) {
          // Monthly path - one task per whole month the range overlapped
          // (already resolved with a real documentId each, in the
          // scheduling phase above).
          for (const d of s.downloads) {
            tasks.push({
              documentId: d.documentId,
              reportType: d.reportType,
              filename: `GST_${t}_${d.monthLabel}.csv`,
              zipFolder: `GST_${t}`,
              kind: 'gst',
              cat: t,
            });
          }
        } else if (s.phase === 'ready') {
          tasks.push({
            documentId: s.documentId,
            dateRangeCovered: s.dateRangeCovered,
            reportType: GST_TYPE_REPORT_TYPE[t],
            filename: buildGstFilename(t, job.fromDate, job.toDate, '.csv'),
            zipFolder: `GST_${t}`,
            kind: 'gst',
            cat: t,
          });
        } else if (s.phase === 'empty') {
          // Confirmed no data for that range/type - not an error, so this
          // is tracked separately from `failures` and never retried (there's
          // nothing a retry would change).
          noData.push({ cat: t, label: t, reason: s.error });
        } else if (s.phase === 'failed') {
          failures.push({ cat: t, label: t, reason: s.error });
        }
      }

      if (!tasks.length) {
        await clearGstJob();
        const parts = [
          ...failures.map((f) => `${f.label}: ${f.reason}`),
          ...noData.map((f) => `${f.label}: no data for that range`),
        ];
        const reason = parts.join(' | ') || 'No report types were selected.';
        const state = newState({ finished: true, error: `No reports were generated. ${reason}` });
        if (failures.length) state.retry = { type: 'gst', types: failures.map((f) => f.cat), fromDate: job.fromDate, toDate: job.toDate };
        await setProgress(state);
        return;
      }

      const state = await processDownloadQueue(tasks, await buildZipName('GST', job.fromDate, job.toDate, job.isRetry, job.tabId), job.tabId);
      if (failures.length && !state.cancelled) {
        // `failures` here are types that never made it into `tasks` at all
        // (failed during scheduling/polling, before processDownloadQueue
        // ever counted them) - state.total only reflects tasks.length, so
        // it has to grow along with state.failed or done+failed can exceed
        // total (e.g. "Downloaded 5/5" with 1 extra failure showing 120%).
        state.failures = [...(state.failures || []), ...failures];
        state.failed = (state.failed || 0) + failures.length;
        state.total = (state.total || 0) + failures.length;
      }
      if (!state.cancelled) {
        const failedTypes = [...new Set((state.failures || []).map((f) => f.cat).filter(Boolean))];
        if (failedTypes.length) {
          state.retry = { type: 'gst', types: failedTypes, fromDate: job.fromDate, toDate: job.toDate };
        }
      }
      await setProgress(state);
      await recordLastSync(`GST ${types.join('/')}`, state);
      await clearGstJob();
      return;
    }
  } catch (e) {
    console.warn(TAG, 'gst sync failed:', e?.message);
    await clearGstJob();
    await setProgress(newState({ finished: true, error: e?.message || 'GST sync failed.' }));
  } finally {
    tickInFlightGst = false;
  }
}

chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === GST_TICK_ALARM_NAME) {
    gstSyncTick().catch((e) => console.error(TAG, 'gst tick failed:', e?.message));
  }
});

async function startGstSync({ types, fromDate, toDate, isRetry, tabId }) {
  const sub = {};
  for (const t of types) sub[t] = { phase: 'scheduling' };
  await setGstJob({
    phase: 'scheduling',
    types, fromDate, toDate, isRetry, tabId,
    sub,
    paused: false,
    cancelled: false,
    startedAt: Date.now(),
  });
  await setProgress(newState({ current: 'Starting sync...' }));
  chrome.alarms.create(GST_TICK_ALARM_NAME, { periodInMinutes: TICK_INTERVAL_MINUTES });
  await gstSyncTick();
}

// ── FBA Customer Returns report engine ──────────────────────────────────────
// Confirmed live: Fulfilment Reports -> FBA customer returns -> Exact dates
// -> Request .csv Download. A different engine from Standard Returns above -
// this rides Amazon's generic "Report Central" API
// (/reportcentral/api/v1/...), with reportFRPId 2610 selecting the Customer
// Returns report specifically. Structurally closest to Payments: schedule
// hands back a real reference id directly (no snapshot/diff needed), and
// polling checks that one id's status - InQueue -> InProgress -> Done.
// No 60-day range cap and no 2-days-back generation-delay cutoff apply here
// (unlike every other engine in this file) - confirmed live, a 14+ month
// range ending on today's date was accepted and generated successfully.

async function scheduleFbaReturnsReport(tabId, { fromDate, toDate }) {
  const resp = await sendToTabWithRecovery(tabId, {
    type: 'SCHEDULE_FBA_RETURNS_REPORT',
    reportStartDate: isoToSlashYMD(fromDate),
    reportEndDate: isoToSlashYMD(toDate),
  });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not request the FBA Returns report.');
  if (resp.csrfMissing) throw new Error("Open Seller Central's FBA customer returns report page (Fulfilment Reports → FBA customer returns) in this tab, then try again.");
  if (resp.status >= 400) throw new Error(`Amazon rejected the request (HTTP ${resp.status}).`);
  const referenceId = resp.body?.reportReferenceId;
  if (!referenceId) throw new Error('Report scheduling did not return a reference id.');
  await cachePageUrl('fbaReturns', resp.pageUrl);
  return referenceId;
}

async function fetchFbaReturnsReportStatus(tabId, referenceId) {
  const resp = await sendToTabWithRecovery(tabId, { type: 'FETCH_FBA_RETURNS_STATUS', referenceId });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not read the FBA Returns report status.');
  if (resp.csrfMissing) throw new Error("Open Seller Central's FBA customer returns report page (Fulfilment Reports → FBA customer returns) in this tab, then try again.");
  if (resp.status >= 400) throw new Error(`Amazon returned HTTP ${resp.status} for the report status.`);
  await cachePageUrl('fbaReturns', resp.pageUrl);
  return Array.isArray(resp.body) ? resp.body[0] : null;
}

// FBA Returns' own individual sync also runs through the Bulk engine now -
// see the note above scheduleReturnsReport's now-removed standalone tick.

// ── Sponsored Ads Reports engine (Sponsored Products -> Advertised product) ─
// A completely different origin (advertising.amazon.in, not
// sellercentral.amazon.in) and a completely different app from every engine
// above - confirmed live through an extensive investigation (a static CSRF
// string search failing twice, then a real breakpoint inside axios's own
// adapter to trace the header back to base.ts's shared HTTP client). Closest
// in shape to Orders/Payments: the create call hands back a real report id
// directly (a bare UUID string, not wrapped in JSON), so this polls that one
// id rather than snapshotting a list like Returns/GST have to.
//
// One real difference from every other engine: the "list" call that reports
// status is a POST with a body (sort/filters/pagination), not a GET - and its
// response's own latestProcessedReportSummary.urlString field hands back the
// exact relative download path once status is "COMPLETED", so there's no
// separate resolve step before downloading, unlike Orders/Payments/GST.
//
// Max lookback: confirmed live at 90 days for Advertised Product (the real
// date picker's earliest selectable date was exactly 90 days back from
// today, 21 May - 18 Aug) - different from Search Term's separately-
// confirmed 65 days, since report types on this app have very different
// caps (e.g. Campaign: 2192 days, Purchased product: 60 days). See
// MAX_ADS_LOOKBACK_DAYS in popup.js. 90 is correct as-is - a live 400 at
// this exact boundary ("Report start date... is beyond the maximum look
// back days: 90") turned out to be a timestamp-construction bug, not a
// wrong day count: comparing our rejected payload against Amazon's own
// client's successful one for the identical calendar range showed Amazon
// sends plain UTC-midnight timestamps for both dates (e.g. reportStartDate
// 1779321600000 = 2026-05-21T00:00:00Z), while this code was appending
// +05:30 (and end-of-day for the end date) - a 5.5-hour offset that pushed
// the start timestamp just past the real floor even on a calendar day
// Amazon's own picker allows. Fixed by matching Amazon's exact construction
// below (plain UTC midnight for both ends) instead of shrinking the range.
// The same 2-days-back generation-delay cutoff used elsewhere is applied by
// direct instruction, not separately confirmed evidence of the same quirk on
// this specific endpoint.

// Ads reports only make sense whole-month to a seller ("August's ad spend"),
// not an arbitrary partial range - so in Bulk sync, the picked date range is
// split into one request per full calendar month it TOUCHES (any overlap,
// even a single day counts - the picked range only decides which months are
// in scope, not how much of a month has to be selected), per two rules
// given by direct instruction:
//  1. The current (still in progress) month is never requested, regardless
//     of whether the picked range includes it - its numbers aren't final yet.
//  2. Any other touched month is requested in FULL (1st through the last
//     day), even if the picked range only touches part of it (e.g. picking
//     21 Jul - 17 Aug still requests the whole of July) - UNLESS the whole
//     month doesn't fit Amazon's 90-day lookback (ADS_LOOKBACK_DAYS, same
//     value as MAX_ADS_LOOKBACK_DAYS in popup.js), in which case that month
//     is skipped entirely rather than requesting a truncated partial month.
// Mirrors gstMonthsInRange's month-walking approach above, just resolving to
// {fromDate, toDate} pairs (and applying the two filters) instead of bare
// month/year labels, since these become real report request ranges.
const ADS_LOOKBACK_DAYS = 90; // matches MAX_ADS_LOOKBACK_DAYS in popup.js

// The three report categories requested per qualifying month in Bulk sync
// (Sponsored TV excluded by direct instruction) - keys match content.js's
// ADS_REPORT_SHAPES and Amazon's own reportCategoryId values.
const ADS_REPORT_KEYS = [
  { key: 'sp', label: 'Sponsored Products' },
  { key: 'hsa', label: 'Sponsored Brands' },
  { key: 'sd', label: 'Sponsored Display' },
];

function adsMonthlyRanges(fromDate, toDate) {
  const pad = (v) => String(v).padStart(2, '0');
  const toIso = (d) => `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;

  const start = new Date(`${fromDate}T00:00:00`);
  const end = new Date(`${toDate}T00:00:00`);
  const today = new Date();
  const currentMonthStart = new Date(today.getFullYear(), today.getMonth(), 1).getTime();
  const lookbackFloor = new Date(today.getFullYear(), today.getMonth(), today.getDate());
  lookbackFloor.setDate(lookbackFloor.getDate() - (ADS_LOOKBACK_DAYS - 1));

  const ranges = [];
  let cursor = new Date(start.getFullYear(), start.getMonth(), 1);
  while (cursor <= end) {
    const monthStart = new Date(cursor.getFullYear(), cursor.getMonth(), 1);
    const monthEnd = new Date(cursor.getFullYear(), cursor.getMonth() + 1, 0); // last day of the month
    const isCurrentMonth = monthStart.getTime() === currentMonthStart;
    const withinLookback = monthStart >= lookbackFloor;
    if (!isCurrentMonth && withinLookback) {
      ranges.push({
        fromDate: toIso(monthStart),
        toDate: toIso(monthEnd),
        label: `${GST_MONTH_NAMES[monthStart.getMonth()]}_${monthStart.getFullYear()}`,
      });
    }
    cursor = new Date(cursor.getFullYear(), cursor.getMonth() + 1, 1);
  }
  return ranges;
}

// reportKey selects which of content.js's ADS_REPORT_SHAPES to request -
// defaults to 'sp' (Sponsored Products -> Advertised product), the original
// single shape, so the standalone Ads tab's behavior is unchanged. Bulk
// sync's own ads branch below passes all three explicitly.
async function scheduleAdsReport(tabId, { fromDate, toDate, reportKey = 'sp' }) {
  const startDate = new Date(`${fromDate}T00:00:00Z`).getTime();
  const endDate = new Date(`${toDate}T00:00:00Z`).getTime();
  const resp = await sendToTabWithRecovery(tabId, { type: 'SCHEDULE_ADS_REPORT', startDate, endDate, reportKey });
  if (!resp?.ok) throw new Error(resp?.error || 'Could not schedule the Ads report.');
  if (resp.entityIdMissing) throw new Error("Open Seller Central's Advertising → Reports page (or the Amazon Ads console's Reports page) in this tab, then try again.");
  if (resp.csrfMissing) throw new Error('Open the Ads Reports page in this tab, then try again.');
  if (resp.status >= 400) throw new Error(`Amazon rejected the request (HTTP ${resp.status}).`);
  if (!resp.reportId) throw new Error('Report scheduling did not return a report id.');
  await cachePageUrl('ads', resp.pageUrl);
  return resp.reportId;
}

let tickInFlightAds = false;
async function adsSyncTick() {
  if (tickInFlightAds) return;
  tickInFlightAds = true;
  let job;
  try {
    job = await getAdsJob();
    if (!job) { await chrome.alarms.clear(ADS_TICK_ALARM_NAME); return; }

    if (job.cancelled) {
      await releaseAdsTab(job);
      await clearAdsJob();
      await setProgress(newState({ finished: true, cancelled: true, current: 'Cancelled - no files were downloaded yet.' }));
      return;
    }
    if (job.paused) {
      await setProgress(newState({ current: 'Paused', paused: true }));
      return;
    }

    // Amazon takes a minimum of ~15 minutes to actually generate this
    // report - per direct instruction, this no longer waits for it at all.
    // 'scheduling' is the only real phase: fire the request, then finish
    // immediately. The seller checks Amazon's own Ads Reports page later to
    // download it manually. recordLastSync is deliberately not called here -
    // it would write "1 file" to the Last Synced bar, which would be wrong
    // since nothing was actually downloaded.
    if (job.phase === 'scheduling') {
      await setProgress(newState({ current: 'Requesting Ads report...' }));
      await scheduleAdsReport(job.tabId, { fromDate: job.fromDate, toDate: job.toDate });
      await releaseAdsTab(job);
      await clearAdsJob();
      await setProgress(newState({
        finished: true, total: 1, done: 1, requestedOnly: true,
        current: 'Report requested.',
        note: 'Amazon takes 15+ minutes to generate this report - open the Ads Reports page (advertising.amazon.in) to download it manually.',
      }));
      return;
    }
  } catch (e) {
    console.warn(TAG, 'ads sync failed:', e?.message);
    await releaseAdsTab(job);
    await clearAdsJob();
    await setProgress(newState({ finished: true, error: e?.message || 'Ads sync failed.' }));
  } finally {
    tickInFlightAds = false;
  }
}

chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === ADS_TICK_ALARM_NAME) {
    adsSyncTick().catch((e) => console.error(TAG, 'ads tick failed:', e?.message));
  }
});

async function startAdsSync({ fromDate, toDate, tabId, navTabId, navTabOwned }) {
  await setAdsJob({
    phase: 'scheduling',
    fromDate, toDate, tabId,
    navTabId: navTabId || null,
    navTabOwned: !!navTabOwned,
    reportId: null,
    paused: false,
    cancelled: false,
    startedAt: Date.now(),
  });
  await setProgress(newState({ current: 'Starting sync...' }));
  chrome.alarms.create(ADS_TICK_ALARM_NAME, { periodInMinutes: TICK_INTERVAL_MINUTES });
  await adsSyncTick();
}

// ── Bulk sync engine (Orders + Payments + B2B/B2C in one pass) ─────────────
// Modeled directly on how the Meesho extension's own bulk sync works: every
// selected category's schedule/create call fires first (one pass through
// the list), then every still-pending category is polled together on each
// tick instead of finishing one completely before starting the next - a
// slow category never blocks a fast one from finishing early - and once
// every category has either succeeded or failed, everything that succeeded
// gets bundled into exactly one ZIP via the same processDownloadQueue() used
// by every individual-category engine above (it already accepts mixed
// task.kind values, so no changes were needed there at all).
//
// Returns (both Standard and FBA) need the exact right Seller Central page
// open - the CSRF hidden input/meta tag only exist there, and Standard
// Returns/FBA Returns are two separate pages that don't initialize each
// other. Rather than requiring the *shared* job.tabId to already be on the
// right page, resolvePageForCategory() (see "Navigation orchestration"
// above sendToTabWithRecovery) resolves a tabId per page-bound category on
// demand - reusing a tab the user already has it open in, or navigating one
// dedicated temporary tab to it (sequentially, since only one page can be
// displayed in a tab at a time) - and only falls back to job.tabId's own
// clear "open this page and try again" error if no URL is known yet at all
// (FBA Returns' page URL is learned automatically the first time that
// happens, not hardcoded - see PAGE_REQUIREMENTS.fbaReturns). Every other
// category (Orders/Payments/GST) has no page requirement and keeps using
// job.tabId directly, unchanged.
//
// Ads is a different origin entirely (advertising.amazon.in, not
// sellercentral.amazon.in), but resolves through the exact same
// resolvePageForCategory system as Returns/FBA Returns (PAGE_REQUIREMENTS.ads
// carries its own originPattern) - it just never enters 'polling' (see the
// scheduling loop below), since per direct instruction this only fires the
// request and doesn't wait ~15+ minutes for Amazon to generate it.
const BULK_CATEGORY_LABELS = { orders: 'Orders', payments: 'Payments', gstB2B: 'GST B2B', gstB2C: 'GST B2C', returns: 'Returns', fbaReturns: 'FBA Returns', ads: 'Ads' };
// gstB2B/gstB2C are two independent categories rather than one 'gst' entry
// with a radio choice - B2B and B2C are just two different reportType values
// against the same create/list endpoints (see GST_TYPE_REPORT_TYPE above),
// so both can run side by side exactly like Returns/FBA Returns already do.
const BULK_GST_META = { gstB2B: { reportType: GST_TYPE_REPORT_TYPE.B2B, label: 'B2B' }, gstB2C: { reportType: GST_TYPE_REPORT_TYPE.B2C, label: 'B2C' } };
function bulkStatusText(categories, sub) {
  return categories.map((c) => {
    const label = BULK_CATEGORY_LABELS[c];
    const phase = sub[c]?.phase;
    if (phase === 'ready') return `${label}: ready`;
    if (phase === 'requested') return `${label}: requested`;
    if (phase === 'empty') return `${label}: no data`;
    if (phase === 'failed') return `${label}: failed`;
    if (phase === 'scheduling') return `${label}: requesting...`;
    return `${label}: waiting...`;
  }).join('  ·  ');
}

let tickInFlightBulk = false;
async function bulkSyncTick() {
  if (tickInFlightBulk) return;
  tickInFlightBulk = true;
  let job;
  try {
    job = await getBulkJob();
    if (!job) { await chrome.alarms.clear(BULK_TICK_ALARM_NAME); return; }

    if (job.cancelled) {
      await releaseNavTab(job);
      await clearBulkJob();
      await setProgress(newState({ finished: true, cancelled: true, current: 'Cancelled - no files were downloaded yet.' }));
      return;
    }
    if (job.paused) {
      // Without `bulk` here, the popup falls back to showing the
      // checkbox-selection grid instead of the frozen live-status view -
      // this keeps the per-category dots visible (just no longer updating)
      // while paused, instead of looking like the sync reset entirely.
      await setProgress(newState({ current: 'Paused', paused: true, bulk: { categories: job.categories, sub: job.sub } }));
      return;
    }

    const { categories } = job;
    const sub = { ...job.sub };

    if (job.phase === 'scheduling') {
      for (const cat of categories) {
        if (sub[cat].phase !== 'scheduling') continue;
        try {
          if (cat === 'orders') {
            const referenceId = await scheduleOrderReport(job.tabId, { startDate: job.fromDate, endDate: job.toDate });
            sub[cat] = { phase: 'polling', referenceId };
          } else if (cat === 'payments') {
            const reportId = await schedulePaymentsReport(job.tabId, { fromDate: job.fromDate, toDate: job.toDate });
            sub[cat] = { phase: 'polling', reportId };
          } else if (BULK_GST_META[cat]) {
            const { reportType, label } = BULK_GST_META[cat];
            if (gstRangeNeedsMonthly(job.fromDate)) {
              // Out of On Demand's 45-day window - resolved synchronously
              // right here (Monthly reports are pre-generated, nothing to
              // poll for), so this jumps straight to 'ready'/'failed',
              // skipping the 'polling' phase entirely for this category.
              const downloads = await resolveGstMonthlyDownloads(job.tabId, { label, fromDate: job.fromDate, toDate: job.toDate });
              if (!downloads.length) sub[cat] = { phase: 'empty', error: 'No GST Monthly reports were available for that date range.' };
              else sub[cat] = { phase: 'ready', downloads };
            } else {
              const before = await fetchGstReportList(job.tabId);
              const beforeSet = matchingGstRecords(before, reportType).map((r) => r.dateRequested);
              await scheduleGstReport(job.tabId, { reportType, fromDate: job.fromDate, toDate: job.toDate });
              sub[cat] = { phase: 'polling', beforeSet };
            }
          } else if (cat === 'returns') {
            const returnsTabId = await resolvePageForCategory(job, cat, 'schedule');
            const before = await fetchReturnsListRecords(returnsTabId);
            const beforeSet = matchingReturnsRecords(before, job.fromDate, job.toDate).map((r) => r.requestedOn);
            await scheduleReturnsReport(returnsTabId, { fromDate: job.fromDate, toDate: job.toDate });
            sub[cat] = { phase: 'polling', beforeSet };
          } else if (cat === 'fbaReturns') {
            const fbaTabId = await resolvePageForCategory(job, cat, 'schedule');
            const referenceId = await scheduleFbaReturnsReport(fbaTabId, { fromDate: job.fromDate, toDate: job.toDate });
            sub[cat] = { phase: 'polling', referenceId };
          } else if (cat === 'ads') {
            // No 'polling' step at all - jumps straight to 'requested', same
            // shape as the GST Monthly branch above skipping polling for a
            // different reason (nothing to poll: Amazon takes ~15+ minutes
            // to generate this report and per direct instruction this
            // doesn't wait for it).
            //
            // One request per full calendar month the picked range covers
            // (see adsMonthlyRanges), times one request per report category
            // in ADS_REPORT_KEYS (Sponsored Products, Sponsored Brands,
            // Sponsored Display) - not one request for the whole range in a
            // single category.
            const adsTabId = await resolvePageForCategory(job, cat, 'schedule');
            const monthRanges = adsMonthlyRanges(job.fromDate, job.toDate);
            if (!monthRanges.length) {
              sub[cat] = { phase: 'empty', error: 'No full calendar month in that range (the current month is always excluded).' };
            } else {
              let requestedCount = 0;
              const totalRequests = monthRanges.length * ADS_REPORT_KEYS.length;
              const failedCombos = [];
              for (const range of monthRanges) {
                for (const { key, label } of ADS_REPORT_KEYS) {
                  const comboLabel = `${label} ${range.label}`;
                  try {
                    const adsReportId = await scheduleAdsReport(adsTabId, { fromDate: range.fromDate, toDate: range.toDate, reportKey: key });
                    console.log(TAG, `bulk: ads report requested for ${comboLabel} - id=${adsReportId} tabId=${adsTabId}`);
                    requestedCount++;
                  } catch (e) {
                    console.warn(TAG, `bulk: ads report request failed for ${comboLabel} -`, e?.message);
                    failedCombos.push(comboLabel);
                  }
                }
              }
              if (!requestedCount) {
                sub[cat] = { phase: 'failed', error: `Could not request any report (${failedCombos.join(', ')}).` };
              } else {
                sub[cat] = { phase: 'requested', requestedCount, totalRequests, totalMonths: monthRanges.length, failedCombos };
              }
            }
          }
        } catch (e) {
          sub[cat] = { phase: 'failed', error: e?.message || 'Could not request this report.' };
          console.warn(TAG, `bulk: ${cat} failed -`, sub[cat].error);
        }
      }
      await setBulkJob({ ...job, sub, phase: 'polling' });
      // `bulk` lets the popup render a live per-category status row (a
      // colored dot/label per selected report type) instead of just one
      // combined text line - it naturally stops appearing once the
      // 'downloading' phase hands off to processDownloadQueue's own generic
      // progress updates, which is the right moment for the popup to switch
      // back to the normal shared progress bar anyway.
      await setProgress(newState({ current: bulkStatusText(categories, sub), bulk: { categories, sub } }));
      return;
    }

    if (job.phase === 'polling') {
      const waitedMs = Date.now() - job.startedAt;
      for (const cat of categories) {
        if (sub[cat].phase !== 'polling') continue;
        try {
          if (cat === 'orders') {
            const jobs = await fetchOrderStatusList(job.tabId);
            const found = jobs.find((j) => j.referenceId === sub[cat].referenceId);
            if (found?.stateReady) sub[cat] = { ...sub[cat], phase: 'ready' };
            else if (found?.stateNoData) sub[cat] = { phase: 'empty', error: 'No orders were found for that date range.' };
            else if (found?.requestStateName && /FAIL|ERROR/i.test(found.requestStateName)) sub[cat] = { phase: 'failed', error: found.requestStateName };
          } else if (cat === 'payments') {
            const status = await fetchPaymentsReportStatus(job.tabId, sub[cat].reportId);
            if (status?.status === 'DOWNLOADABLE') sub[cat] = { ...sub[cat], phase: 'ready' };
            else if (status?.status && /CANCEL|FAIL|ERROR/i.test(status.status)) sub[cat] = { phase: 'failed', error: `Payments report ${status.status.toLowerCase()}.` };
          } else if (BULK_GST_META[cat]) {
            const { reportType } = BULK_GST_META[cat];
            const records = await fetchGstReportList(job.tabId);
            const candidates = matchingGstRecords(records, reportType).filter((r) => !sub[cat].beforeSet.includes(r.dateRequested));
            if (candidates.length) {
              const record = candidates[0];
              if (record.reportStatus === 'Done') {
                if (!record.reportDocumentId) sub[cat] = { phase: 'empty', error: 'No records were found for that date range.' };
                else sub[cat] = { ...sub[cat], phase: 'ready', documentId: record.reportDocumentId, dateRangeCovered: (record.dateRangeCovered || []).join(' - ') };
              } else if (record.reportStatus && /FAIL|ERROR/i.test(record.reportStatus)) {
                sub[cat] = { phase: 'failed', error: `GST report ${record.reportStatus.toLowerCase()}.` };
              }
            }
          } else if (cat === 'returns') {
            const records = await fetchReturnsListRecords(job.tabId);
            const candidates = matchingReturnsRecords(records, job.fromDate, job.toDate).filter((r) => !sub[cat].beforeSet.includes(r.requestedOn));
            if (candidates.length) {
              const record = candidates[0];
              if (record.tsv.status === 'ready') sub[cat] = { ...sub[cat], phase: 'ready', tsvUrl: record.tsv.url };
              else if (record.tsv.status === 'no_records') sub[cat] = { phase: 'empty', error: 'No returns were found for that date range.' };
            }
          } else if (cat === 'fbaReturns') {
            const fbaTabId = await resolvePageForCategory(job, cat, 'poll');
            const status = await fetchFbaReturnsReportStatus(fbaTabId, sub[cat].referenceId);
            if (status === 'Done') sub[cat] = { ...sub[cat], phase: 'ready' };
            else if (status && /FAIL|ERROR|CANCEL/i.test(status)) sub[cat] = { phase: 'failed', error: `FBA Returns report ${status.toLowerCase()}.` };
          }
        } catch (e) {
          sub[cat] = { phase: 'failed', error: e?.message || 'Could not read this report\'s status.' };
        }
        if (sub[cat].phase === 'polling' && waitedMs > 50 * 60 * 1000) {
          sub[cat] = { phase: 'failed', error: 'Did not finish generating within the expected time.' };
        }
      }

      const stillPolling = categories.some((c) => sub[c].phase === 'polling');
      if (!stillPolling) {
        // Nothing left that could still need a page-bound tab from here on -
        // downloading needs no CSRF for any category, so release it now
        // rather than leaving it open for the rest of the run.
        await releaseNavTab(job);
        await setBulkJob({ ...job, sub, phase: 'downloading' });
        return;
      }
      await setBulkJob({ ...job, sub });
      // `bulk` lets the popup render a live per-category status row (a
      // colored dot/label per selected report type) instead of just one
      // combined text line - it naturally stops appearing once the
      // 'downloading' phase hands off to processDownloadQueue's own generic
      // progress updates, which is the right moment for the popup to switch
      // back to the normal shared progress bar anyway.
      await setProgress(newState({ current: bulkStatusText(categories, sub), bulk: { categories, sub } }));
      return;
    }

    if (job.phase === 'downloading') {
      const tasks = [];
      const failures = [];
      const noData = [];
      const requested = [];
      for (const cat of categories) {
        const s = sub[cat];
        if (s.phase === 'ready') {
          if (cat === 'orders') {
            tasks.push({ referenceId: s.referenceId, filename: buildOrderFilename(job.fromDate, job.toDate, '.txt'), zipFolder: 'Orders', cat });
          } else if (cat === 'payments') {
            tasks.push({ reportId: s.reportId, filename: buildPaymentsFilename(job.fromDate, job.toDate, '.csv'), zipFolder: 'Payments', kind: 'payments', cat });
          } else if (BULK_GST_META[cat]) {
            const { reportType, label } = BULK_GST_META[cat];
            if (s.downloads) {
              // Monthly path - one task per whole month the range
              // overlapped (already resolved with a real documentId each,
              // in the scheduling phase above).
              for (const d of s.downloads) {
                tasks.push({
                  documentId: d.documentId, reportType: d.reportType,
                  filename: `GST_${label}_${d.monthLabel}.csv`, zipFolder: `GST_${label}`, kind: 'gst', cat,
                });
              }
            } else {
              tasks.push({
                documentId: s.documentId, dateRangeCovered: s.dateRangeCovered, reportType,
                filename: buildGstFilename(label, job.fromDate, job.toDate, '.csv'), zipFolder: `GST_${label}`, kind: 'gst', cat,
              });
            }
          } else if (cat === 'returns') {
            tasks.push({ url: s.tsvUrl, filename: buildReturnsFilename(job.fromDate, job.toDate, '.tsv'), zipFolder: 'Returns', kind: 'returns', cat });
          } else if (cat === 'fbaReturns') {
            tasks.push({ referenceId: s.referenceId, filename: buildFbaReturnsFilename(job.fromDate, job.toDate, '.csv'), zipFolder: 'FBA_Returns', kind: 'fbaReturns', cat });
          }
        } else if (s.phase === 'requested') {
          requested.push({ cat, label: BULK_CATEGORY_LABELS[cat], requestCount: s.requestedCount || null });
        } else if (s.phase === 'empty') {
          noData.push({ cat, label: BULK_CATEGORY_LABELS[cat], reason: s.error });
        } else if (s.phase === 'failed') {
          failures.push({ cat, label: BULK_CATEGORY_LABELS[cat], reason: s.error });
        }
      }

      if (!tasks.length && !requested.length) {
        await clearBulkJob();
        const parts = [
          ...failures.map((f) => `${f.label}: ${f.reason}`),
          ...noData.map((f) => `${f.label}: no data for that range`),
        ];
        const reason = parts.join(' | ') || 'No report types were selected.';
        const state = newState({ finished: true, error: `No reports were generated. ${reason}` });
        if (failures.length) state.retry = { type: 'bulk', categories: failures.map((f) => f.cat), fromDate: job.fromDate, toDate: job.toDate, label: job.label };
        await setProgress(state);
        return;
      }

      const requestedNote = requested.length
        ? requested.map((r) => {
            const countText = r.requestCount ? `${r.requestCount} report${r.requestCount > 1 ? 's' : ''} requested (Sponsored Products/Brands/Display × qualifying months)` : 'report requested';
            return `${r.label}: ${countText} - Amazon takes 15+ minutes to generate each, check the Reports page to download them manually.`;
          }).join(' | ')
        : null;

      if (!tasks.length) {
        // Only requested-only categories succeeded (e.g. a Bulk run with
        // just Ads selected) - nothing to actually download, so skip
        // processDownloadQueue (it would just no-op with done:0) and report
        // success directly, same shape as the standalone Ads path.
        await releaseNavTab(job);
        await clearBulkJob();
        await setProgress(newState({ finished: true, total: 1, done: 1, requestedOnly: true, current: 'Report requested.', note: requestedNote }));
        return;
      }

      const state = await processDownloadQueue(tasks, await buildZipName(job.label || 'Bulk', job.fromDate, job.toDate, job.isRetry, job.tabId), job.tabId);
      if (requestedNote) state.note = requestedNote;
      if (failures.length && !state.cancelled) {
        // `failures` here are categories that never made it into `tasks` at
        // all (failed during scheduling/polling, before processDownloadQueue
        // ever counted them) - state.total only reflects tasks.length, so it
        // has to grow along with state.failed or done+failed can exceed
        // total (e.g. "Downloaded 5/5" with 1 extra failure showing 120%).
        state.failures = [...(state.failures || []), ...failures];
        state.failed = (state.failed || 0) + failures.length;
        state.total = (state.total || 0) + failures.length;
      }
      // Every failure by this point (scheduling/polling-stage `failures`, or
      // a task's own download-stage failure via its `cat`) maps back to a
      // real category key - "Retry failed only" just re-runs START_BULK_SYNC
      // scoped to whichever of those categories didn't make it into the ZIP.
      if (!state.cancelled) {
        const failedCats = [...new Set((state.failures || []).map((f) => f.cat).filter(Boolean))];
        if (failedCats.length) {
          state.retry = { type: 'bulk', categories: failedCats, fromDate: job.fromDate, toDate: job.toDate, label: job.label };
        }
      }
      await setProgress(state);
      await recordLastSync(categories.map((c) => BULK_CATEGORY_LABELS[c]).join(', '), state);
      await clearBulkJob();
      return;
    }
  } catch (e) {
    console.warn(TAG, 'bulk sync failed:', e?.message);
    if (job) await releaseNavTab(job);
    await clearBulkJob();
    await setProgress(newState({ finished: true, error: e?.message || 'Bulk sync failed.' }));
  } finally {
    tickInFlightBulk = false;
  }
}

chrome.alarms.onAlarm.addListener((alarm) => {
  if (alarm.name === BULK_TICK_ALARM_NAME) {
    bulkSyncTick().catch((e) => console.error(TAG, 'bulk tick failed:', e?.message));
  }
});

async function startBulkSync({ categories, fromDate, toDate, label, isRetry, tabId }) {
  const sub = {};
  for (const cat of categories) sub[cat] = { phase: 'scheduling' };
  await setBulkJob({
    phase: 'scheduling',
    categories, fromDate, toDate, label, isRetry, tabId,
    sub,
    navTabId: null, navTabOwned: false, navTabFor: null,
    paused: false,
    cancelled: false,
    startedAt: Date.now(),
  });
  await setProgress(newState({ current: 'Starting bulk sync...' }));
  chrome.alarms.create(BULK_TICK_ALARM_NAME, { periodInMinutes: TICK_INTERVAL_MINUTES });
  await bulkSyncTick();
}

chrome.runtime.onMessage.addListener((msg, _sender, sendResponse) => {
  if (msg?.type === 'CLEAR_BADGE') {
    chrome.action.setBadgeText({ text: '' }).catch(() => {});
    sendResponse({ ok: true });
    return false;
  }

  if (msg?.type === 'AUTH_LOGIN') {
    (async () => {
      try {
        const user = await authLogin(msg.email, msg.password);
        sendResponse({ ok: true, user });
      } catch (e) {
        console.warn(TAG, 'login failed:', e?.message);
        sendResponse({ ok: false, error: e?.message || 'Login failed.' });
      }
    })();
    return true; // keep the channel open for the async sendResponse above
  }

  if (msg?.type === 'AUTH_REFRESH_PROFILE') {
    (async () => {
      try {
        const { auth } = await chrome.storage.local.get('auth');
        if (!auth?.token) { sendResponse({ ok: false }); return; }
        const user = await authFetchProfile(auth.token);
        await chrome.storage.local.set({ auth: { ...auth, user } });
        sendResponse({ ok: true, user });
      } catch (e) {
        // Never clear the stored token here - a transient network/profile
        // error must not log the user out.
        sendResponse({ ok: false });
      }
    })();
    return true;
  }

  if (msg?.type === 'PAUSE_SYNC') {
    (async () => {
      const oJob = await getOrderJob();
      if (oJob) { await setOrderJob({ ...oJob, paused: true }); orderSyncTick().catch((e) => console.error(TAG, 'tick failed:', e?.message)); }
      const pJob = await getPaymentsJob();
      if (pJob) { await setPaymentsJob({ ...pJob, paused: true }); paymentsSyncTick().catch((e) => console.error(TAG, 'payments tick failed:', e?.message)); }
      const gJob = await getGstJob();
      if (gJob) { await setGstJob({ ...gJob, paused: true }); gstSyncTick().catch((e) => console.error(TAG, 'gst tick failed:', e?.message)); }
      const aJob = await getAdsJob();
      if (aJob) { await setAdsJob({ ...aJob, paused: true }); adsSyncTick().catch((e) => console.error(TAG, 'ads tick failed:', e?.message)); }
      const bJob = await getBulkJob();
      if (bJob) { await setBulkJob({ ...bJob, paused: true }); bulkSyncTick().catch((e) => console.error(TAG, 'bulk tick failed:', e?.message)); }
      sendResponse({ ok: true });
    })();
    return true;
  }
  if (msg?.type === 'RESUME_SYNC') {
    (async () => {
      const oJob = await getOrderJob();
      if (oJob) { await setOrderJob({ ...oJob, paused: false }); orderSyncTick().catch((e) => console.error(TAG, 'tick failed:', e?.message)); }
      const pJob = await getPaymentsJob();
      if (pJob) { await setPaymentsJob({ ...pJob, paused: false }); paymentsSyncTick().catch((e) => console.error(TAG, 'payments tick failed:', e?.message)); }
      const gJob = await getGstJob();
      if (gJob) { await setGstJob({ ...gJob, paused: false }); gstSyncTick().catch((e) => console.error(TAG, 'gst tick failed:', e?.message)); }
      const aJob = await getAdsJob();
      if (aJob) { await setAdsJob({ ...aJob, paused: false }); adsSyncTick().catch((e) => console.error(TAG, 'ads tick failed:', e?.message)); }
      const bJob = await getBulkJob();
      if (bJob) { await setBulkJob({ ...bJob, paused: false }); bulkSyncTick().catch((e) => console.error(TAG, 'bulk tick failed:', e?.message)); }
      sendResponse({ ok: true });
    })();
    return true;
  }
  // Sent by the popup on a short timer (every few seconds) while it's open
  // and a sync is active/unfinished. The alarm above is the reliable
  // background safety net (keeps the sync going even if the popup is
  // closed), but Chrome enforces a real floor around 1 minute on alarm
  // periods regardless of what periodInMinutes asks for - which made status
  // (e.g. "Done" in Amazon's own UI) sit stale in the popup for up to a
  // minute at a time, only ever refreshing right away when Pause/Resume
  // forced an immediate tick. This gives the popup that same immediate-tick
  // path on its own timer, so watching the popup live is actually live,
  // without weakening the alarm as the fallback for when it's closed.
  if (msg?.type === 'POLL_NOW') {
    (async () => {
      const oJob = await getOrderJob();
      if (oJob && !oJob.paused && !oJob.cancelled) orderSyncTick().catch((e) => console.error(TAG, 'tick failed:', e?.message));
      const pJob = await getPaymentsJob();
      if (pJob && !pJob.paused && !pJob.cancelled) paymentsSyncTick().catch((e) => console.error(TAG, 'payments tick failed:', e?.message));
      const gJob = await getGstJob();
      if (gJob && !gJob.paused && !gJob.cancelled) gstSyncTick().catch((e) => console.error(TAG, 'gst tick failed:', e?.message));
      const aJob = await getAdsJob();
      if (aJob && !aJob.paused && !aJob.cancelled) adsSyncTick().catch((e) => console.error(TAG, 'ads tick failed:', e?.message));
      const bJob = await getBulkJob();
      if (bJob && !bJob.paused && !bJob.cancelled) bulkSyncTick().catch((e) => console.error(TAG, 'bulk tick failed:', e?.message));
      sendResponse({ ok: true });
    })();
    return true;
  }

  if (msg?.type === 'CANCEL_SYNC') {
    (async () => {
      const oJob = await getOrderJob();
      if (oJob) { await setOrderJob({ ...oJob, cancelled: true, paused: false }); orderSyncTick().catch((e) => console.error(TAG, 'tick failed:', e?.message)); }
      const pJob = await getPaymentsJob();
      if (pJob) { await setPaymentsJob({ ...pJob, cancelled: true, paused: false }); paymentsSyncTick().catch((e) => console.error(TAG, 'payments tick failed:', e?.message)); }
      const gJob = await getGstJob();
      if (gJob) { await setGstJob({ ...gJob, cancelled: true, paused: false }); gstSyncTick().catch((e) => console.error(TAG, 'gst tick failed:', e?.message)); }
      const aJob = await getAdsJob();
      if (aJob) { await setAdsJob({ ...aJob, cancelled: true, paused: false }); adsSyncTick().catch((e) => console.error(TAG, 'ads tick failed:', e?.message)); }
      const bJob = await getBulkJob();
      if (bJob) { await setBulkJob({ ...bJob, cancelled: true, paused: false }); bulkSyncTick().catch((e) => console.error(TAG, 'bulk tick failed:', e?.message)); }
      sendResponse({ ok: true });
    })();
    return true;
  }

  if (msg?.type === 'START_ORDER_SYNC') {
    (async () => {
      try {
        await setProgress(newState({ current: 'Starting sync...' }));

        const tabs = await chrome.tabs.query({ url: 'https://sellercentral.amazon.in/*' });
        if (!tabs.length) {
          await setProgress(newState({ finished: true, error: 'Open Amazon Seller Central (logged in) first.' }));
          return;
        }
        const tab = tabs.find((t) => t.active) || tabs[0];

        if (!(await ensureContentScript(tab.id))) {
          await setProgress(newState({ finished: true, error: 'Could not connect to the Seller Central tab. Please reload the page and try again.' }));
          return;
        }

        const { fromDate, toDate } = msg;
        if (!fromDate || !toDate) {
          await setProgress(newState({ finished: true, error: 'Pick a date range first.' }));
          return;
        }
        if (fromDate > toDate) {
          await setProgress(newState({ finished: true, error: 'From date must be before the To date.' }));
          return;
        }

        console.log(TAG, `starting order sync: ${fromDate} to ${toDate}`);
        await startOrderSync({ fromDate, toDate, tabId: tab.id });
      } catch (e) {
        console.warn(TAG, 'sync failed to start:', e?.message);
        await setProgress(newState({ finished: true, error: e?.message || 'Sync failed.' }));
      }
    })();
    sendResponse({ ok: true });
    return false;
  }

  if (msg?.type === 'START_PAYMENTS_SYNC') {
    (async () => {
      try {
        await setProgress(newState({ current: 'Starting sync...' }));

        const tabs = await chrome.tabs.query({ url: 'https://sellercentral.amazon.in/*' });
        if (!tabs.length) {
          await setProgress(newState({ finished: true, error: 'Open Amazon Seller Central (logged in) first.' }));
          return;
        }
        const tab = tabs.find((t) => t.active) || tabs[0];

        if (!(await ensureContentScript(tab.id))) {
          await setProgress(newState({ finished: true, error: 'Could not connect to the Seller Central tab. Please reload the page and try again.' }));
          return;
        }

        const { fromDate, toDate } = msg;
        if (!fromDate || !toDate) {
          await setProgress(newState({ finished: true, error: 'Pick a date range first.' }));
          return;
        }
        if (fromDate > toDate) {
          await setProgress(newState({ finished: true, error: 'From date must be before the To date.' }));
          return;
        }

        console.log(TAG, `starting payments sync: ${fromDate} to ${toDate}`);
        await startPaymentsSync({ fromDate, toDate, tabId: tab.id });
      } catch (e) {
        console.warn(TAG, 'payments sync failed to start:', e?.message);
        await setProgress(newState({ finished: true, error: e?.message || 'Sync failed.' }));
      }
    })();
    sendResponse({ ok: true });
    return false;
  }

  if (msg?.type === 'START_GST_SYNC') {
    (async () => {
      try {
        await setProgress(newState({ current: 'Starting sync...' }));

        const tabs = await chrome.tabs.query({ url: 'https://sellercentral.amazon.in/*' });
        if (!tabs.length) {
          await setProgress(newState({ finished: true, error: 'Open Amazon Seller Central (logged in) first.' }));
          return;
        }
        const tab = tabs.find((t) => t.active) || tabs[0];

        if (!(await ensureContentScript(tab.id))) {
          await setProgress(newState({ finished: true, error: 'Could not connect to the Seller Central tab. Please reload the page and try again.' }));
          return;
        }

        const { types, fromDate, toDate, isRetry } = msg;
        if (!Array.isArray(types) || !types.length) {
          await setProgress(newState({ finished: true, error: 'Pick B2B and/or B2C first.' }));
          return;
        }
        if (!fromDate || !toDate) {
          await setProgress(newState({ finished: true, error: 'Pick a date range first.' }));
          return;
        }
        if (fromDate > toDate) {
          await setProgress(newState({ finished: true, error: 'From date must be before the To date.' }));
          return;
        }

        console.log(TAG, `starting gst sync: ${types.join('+')} ${fromDate} to ${toDate}`);
        await startGstSync({ types, fromDate, toDate, isRetry, tabId: tab.id });
      } catch (e) {
        console.warn(TAG, 'gst sync failed to start:', e?.message);
        await setProgress(newState({ finished: true, error: e?.message || 'Sync failed.' }));
      }
    })();
    sendResponse({ ok: true });
    return false;
  }

  if (msg?.type === 'START_ADS_SYNC') {
    (async () => {
      try {
        await setProgress(newState({ current: 'Starting sync...' }));

        // A different origin from every other category - the Ads Reports
        // page lives on advertising.amazon.in, not sellercentral.amazon.in.
        // resolveAdsTab reuses an already-open tab, or silently opens/reuses
        // one from a previously-learned URL (see PAGE_REQUIREMENTS.ads) -
        // only the very first-ever run (nothing learned yet) needs the user
        // to have opened the page manually.
        const { tabId: resolvedTabId, opened } = await resolveAdsTab();
        if (!resolvedTabId) {
          await setProgress(newState({ finished: true, error: 'Open the Amazon Ads Reports page (advertising.amazon.in) first - via Seller Central → Reports → Advertising. After that, future syncs will open it automatically.' }));
          return;
        }

        if (!(await ensureContentScript(resolvedTabId))) {
          await setProgress(newState({ finished: true, error: 'Could not connect to the Ads Reports tab. Please reload the page and try again.' }));
          return;
        }

        const { fromDate, toDate } = msg;
        if (!fromDate || !toDate) {
          await setProgress(newState({ finished: true, error: 'Pick a date range first.' }));
          return;
        }
        if (fromDate > toDate) {
          await setProgress(newState({ finished: true, error: 'From date must be before the To date.' }));
          return;
        }

        console.log(TAG, `starting ads sync: ${fromDate} to ${toDate}`);
        await startAdsSync({ fromDate, toDate, tabId: resolvedTabId, navTabId: opened ? resolvedTabId : null, navTabOwned: opened });
      } catch (e) {
        console.warn(TAG, 'ads sync failed to start:', e?.message);
        await setProgress(newState({ finished: true, error: e?.message || 'Sync failed.' }));
      }
    })();
    sendResponse({ ok: true });
    return false;
  }

  if (msg?.type === 'START_BULK_SYNC') {
    (async () => {
      try {
        await setProgress(newState({ current: 'Starting bulk sync...' }));

        const tabs = await chrome.tabs.query({ url: 'https://sellercentral.amazon.in/*' });
        if (!tabs.length) {
          await setProgress(newState({ finished: true, error: 'Open Amazon Seller Central (logged in) first.' }));
          return;
        }
        const tab = tabs.find((t) => t.active) || tabs[0];

        if (!(await ensureContentScript(tab.id))) {
          await setProgress(newState({ finished: true, error: 'Could not connect to the Seller Central tab. Please reload the page and try again.' }));
          return;
        }

        const { categories, fromDate, toDate, label, isRetry } = msg;
        if (!Array.isArray(categories) || !categories.length) {
          await setProgress(newState({ finished: true, error: 'Pick at least one report type first.' }));
          return;
        }
        if (!fromDate || !toDate) {
          await setProgress(newState({ finished: true, error: 'Pick a date range first.' }));
          return;
        }
        if (fromDate > toDate) {
          await setProgress(newState({ finished: true, error: 'From date must be before the To date.' }));
          return;
        }

        console.log(TAG, `starting bulk sync: ${categories.join(',')} ${fromDate} to ${toDate}`);
        await startBulkSync({ categories, fromDate, toDate, label, isRetry, tabId: tab.id });
      } catch (e) {
        console.warn(TAG, 'bulk sync failed to start:', e?.message);
        await setProgress(newState({ finished: true, error: e?.message || 'Sync failed.' }));
      }
    })();
    sendResponse({ ok: true });
    return false;
  }
  return false;
});

})();
